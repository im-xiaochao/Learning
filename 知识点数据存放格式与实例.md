# 词数同行：知识点数据存放格式与实例

版本：1.6
日期：2026-09-17
依据：更新后的「词数同行小程序」设计稿，入口文件 `ci-shu-tong-xing.html`。

> **v1.6 相对 v1.5 的变化**（设计稿与应用侧**均已落地**）
>
> 1. **数学 tab 的三个学科切换改用胶囊 tab**。原来是下划线 tab（`.subject-tabs`），
>    现在与政治卷的「4套卷 / 8套卷」**共用同一套控件**（`.src-switch` / `.src-chip`）。
>    资料库仍用下划线 tab——5 个学科塞进胶囊会挤成一排碎块，那种密度下下划线更清楚。
> 2. **数学 tab 的标签改用学科全称**：高等数学 / 线性代数 / **概率论**。
>    原先按钮上放的是简称（高数 / 线代 / 概率，字段 `shortName`），
>    胶囊只有三个、横向放得下，用全称更好认。
>    **概率统计因此改名 `概率论`**（学科 `name`），设计稿与应用侧的学科名要一致。
> 3. 学科切换器的渲染收进一个函数 `subjectSwitch(tab, list)`，按 `tab` 分流两种外观，
>    两种外观都带同一个锚点 `data-od-id="subject-switch"`，测试台据此断言。
>    注意：**不能按 class 找学科切换器**——政治页里本来就有一个 `.src-switch`
>    （那是题库的来源切换，不是学科切换）。
>
> 应用侧怎么落地的（两处关键决定）：
>
> - **外观由页面显式声明，不按学科个数猜**：`KnowledgeLibrary` 新增
>   `switchStyle: 'pill' | 'underline'`（默认 `underline`），`pages/math/math.vue` 传 `pill`，
>   资料库不传。标签随之取 `name`（胶囊）或 `shortName`（下划线）。
>   「为什么长这样」是页面的设计决定，不该从数据里的学科个数反推。
> - **共用的样式只定义一次**：`.src-switch` / `.src-chip` 从 `components/PoliticsQuiz.vue`
>   上提到 `App.vue`（全局），两个宿主共用一份。`check-politics-style.mjs` 的映射表随之
>   把这三条登记到 `App.vue` 名下。
>
> ⚠️ **一处已知偏离（有意保留）**：资料库那 5 个学科，应用侧是「白底描边胶囊」
> （`.subject-tab`：`border-radius` + `border` + 选中填 primary），**不是**设计稿里的
> 「下划线 tab」。原因是 5 个学科挤一行时，应用侧的等分胶囊比设计稿的 5 列网格更好读，
> 且这个形态早在 v1.6 之前就是应用侧的样子。这项**没有登记进样式检查脚本**
> （它只审 MAP 里的选择器），所以不会被误报，但也没被守——要统一形态时以设计稿为准。
>
> **v1.5 相对 v1.4 的变化**
>
> 1. **政治改成两级结构：套卷卡在主包，题干在分包详情页。** v1.4 把题目列表直接内联在
>    资料库的政治学科下面，代价是**题干必须常驻主包**——`politics-index.ts` 因此长到 202 KB，
>    而且会随题库继续增长（主包上限 2 MB，这是真会被顶爆的地方）。
>    设计稿现在拆成两层：`politicsQuizBody()` 只画「来源切换 + 套卷卡」，点某套卷进
>    独立的 `quizPaperSetPage()`（路由 `quiz-paper`），**题干只在这一页出现**。
> 2. 主包的政治清单投影**只留题目 id**：`appPoliticsPaperSets`（每套卷一组 id，含卷面顺序）。
>    `stem` / `type` / `module` / `difficulty` 全部裁掉——它们只服务于已经去掉的列表行。
>    文件从 202 KB 降到 8.6 KB，且不随题干长度变化。
>    题量由 `ids.length` 得来，「已练 M 题」由 id 去查同学作答记录。
> 3. **「考纲模块」来源整条去掉**：来源切换只剩 4套卷 / 8套卷，
>    `appPoliticsIndexModuleOrder` 随之删除（主包不再需要模块顺序）。
>    题库里那 6 道早期示意题（没有 `book` / `set`）因此没有任何界面入口，仍留在题库里存底。
> 4. 应用侧新增 `pages-politics/paper/paper.vue`（分包内的卷详情页），
>    并在第 11 节加了第 16 条断言：**主包组件不得渲染题干**。
> 5. 材料题在卷详情页里的标记改成「已读参考答案」：材料题没有对错，
>    按 `correct:false` 标成「答错了」是设计稿早期的一处缺陷，已修（两侧一起）。
>
> **v1.4 相对 v1.3 的变化**
>
> 1. **资料库的政治模块不再跳页**。设计稿里政治**不是独立页面**——`knowledgePage()`
>    选中政治时直接把刷题内容渲染在学科 tab 下面
>    （`isQuiz ? politicsQuizBody() : knowledgeResults()`）。应用侧照做：
>    政治学科的内容**就是**刷题内容，不再放「进入政治刷题」的入口卡。
> 2. 为此主包新增一份**政治题目清单投影** `data/generated/app/politics-index.ts`
>    （`appPoliticsIndex` + `appPoliticsIndexModuleOrder`）。它只含列表要画的字段：
>    `id` / `type` / `module` / `difficulty` / `stem` / `book` / `set`；
>    **不含** `options` / `answerKey(s)` / `explanation` / `material` / `answerPoints`。
>    题库正文仍在 `pages-politics/questions.ts` 分包内——这份清单是
>    「主包不能 import 分包数据」这条硬约束下，把内容渲染进主包页面的唯一可行做法
>    （与单词侧的 `word-ids.ts` 同一个套路，只是字段多一些）。
> 3. 第 10 节补：**清单里的题干按原文保留，不按界面显示的截断长度裁剪**。
>    界面要短就用 CSS 截断，数据层一裁就回不来了。
> 4. 题型判定抽出主包共用函数 `utils/politics.ts` 的 `isObjective(type)`
>    （单选 + 多选）：`type === 'choice'` 这种写法在加多选后已在 3 处静默漏判过。

> **v1.3 相对 v1.2 的变化**
>
> 1. **政治题库新增「多选题」题型**（`type: 'multi'`）。起因：录入肖四肖八真题时发现
>    多选题（答案形如 `ABD`）在 v1.2 的契约里存不下——`answerKey` 是单个字符串，
>    校验器又要求它必须命中某个选项 key，多选答案会被直接判为非法。
> 2. **多选答案存在 `answerKeys: string[]`**（至少 2 项、不重复、须全部命中选项），
>    **与单选的 `answerKey` 互斥**：单选写了 `answerKeys`、或多选写了 `answerKey`，
>    校验器**报错**（不是告警）。避免「答案存了但判分读另一个字段」的静默错误。
> 3. **多选判分是「完全一致」**：所选集合与 `answerKeys` 集合相等才算对。
> 4. 第 7 节补 `quizProgress.picked` 在多选题下的表示：所选 key **排序后拼接**成字符串
>    （选 A、C 存 `"AC"`），使三种题型共用同一个 string 字段。
> 5. 第 10 节投影表补 `answerKeys[]` 的保留规则。
> 6. 校验新增多选相关的 7 条负例（见 `tools/_test-politics-rules.mjs`）。
> 7. **第 8 节补刷题的统计口径**：刷题**不计入今日目标**（进度环与待办仍是单词 + 知识点两项），
>    但进入「已练题目」累计统计与周图表的第三种模式；首页加一张整行的「政治刷题」入口卡。

> **v1.2 相对 v1.1 的变化**
>
> 1. **政治改为刷题模块**。政治暂无可发布的知识点讲解，手上只有题目（选择题 + 材料题），
>    因此政治不做知识点列表、改做刷题：新增第 5 节「政治题库格式」（与知识点并列的一套结构），
>    以及 4.3 的判据（`kind === 'politics'` **且**该学科无章节 → 走刷题）。
>    政治的 `chapters.ts` 已清空为 `[]`。
> 2. **修正 v1.1 的一处自相矛盾**：原第 3 节写「数学、政治与计算机专业课均为讲解内容，
>    不设计题目字段」，但政治恰恰只有题目。现改为：**知识点格式里永远没有题目字段；
>    题目只存在于政治题库这一套独立结构里**（例外情况已在第 3 节标注）。
> 3. **第 5 节补全政治题库的字段约束**：`id` 只能含 `[a-z0-9-]`、`module` 取
>    `POLITICS_MODULES` 白名单、`difficulty` ∈ {1,2,3}、`tags` 必填为数组、
>    选择题选项 ≥2 且 key 不重复、`answerKey` 必须命中选项、材料题段落与采分点非空。
>    这些规则已用负例脚本逐条验证确实生效（见 `tools/_test-politics-rules.mjs`）。
> 4. **第 10 节补政治题库的投影规则**：未用到的半套字段整键省略
>    （选择题不写 `material` / `answerPoints`），`options` 保留成 `{key, text}`
>    而非纯字符串——判对错要按 key 比对。
> 5. **政治题库运行数据落在分包** `pages-politics/questions.ts`，
>    刷题会话状态 `usePoliticsQuiz.ts` 也在分包内（它 import 题库，放主包会把题库拉回主包）。
> 6. 校验新增第 11 条：政治题库的 id / module / answerKey / 选项 key / 材料完整性，
>    以及分包落位断言（`pages-politics/questions.ts` 必须存在且含每个 `q.id`）。

> **v1.1 相对 v1.0 的变化**
>
> 1. **目录从 3 课程 4 学科扩到 4 课程 8 学科**：新增「计算机专业课」课程与
>    组成原理 / 操作系统 / 数据结构 / 计算机网络四门学科。tabBar 改为
>    **学习 / 单词 / 数学 / 资料库 / 我的**（5 个，微信原生上限），
>    数学与资料库是两个 tab，共用一套知识库页面，靠学科清单区分。
> 2. **学科新增必填字段 `kind`**（`math` / `politics` / `cs`），替代原先用
>    `subjectId === 'politics'` 硬编码判断「概念抓手 / 关键公式」与插图的写法。
> 3. **知识点新增 `sectionId` / `sectionTitle`**：章节下多一层「小节」，
>    与设计稿「第一章 / 已读 N 节」的展示口径对齐。
> 4. **章节新增 `module`**：承载「数学一 / 数学二」等考纲范围，用于列表分组与搜索。
>    后续补充：数学一与数学二**共用同一套章节**，原先两份并列导致线性代数出现
>    12 章（实际 6 章）、高数出现同名章节重复。已改为共用章节只存一份，
>    新增 `modules: string[]` 标注适用范围，前端加「数学一 / 数学二」筛选。
>    合并后章节 35 → 25，知识点 963 → 904（合并了 59 条完全重复的条目）。
> 5. **知识点正文改为按章节打包**（一章一文件，内含 `knowledge[]`），
>    匹配实际的内容规模（963 个知识点；一章最多 29 个）。
> 6. **新增第 10 节「运行数据投影」**：记录规范数据到小程序运行时数据的字段裁剪规则
>    与分包边界，这是落地时踩过坑的部分。
> 7. 补齐 v1.0 未写明的实现约束：`keyPoints` 恒 3 条、`examples` 恒 1 条、
>    `anchor.format` 实际恒为 `text`、`sectionId` 按 `-sN` 规则可推导。

## 1. 推荐方案

采用 **JSON 存结构化数据，Markdown 存讲解正文，LaTeX 存数学公式**。Markdown 正文和 LaTeX 公式直接放在 JSON 字符串字段里，第一版无需为每个知识点另建正文文件。

数学、政治与计算机专业课使用统一的知识点格式；单词使用独立格式。公共内容与个人记录分别保存，用稳定 ID 关联。学习首页、数学、资料库、搜索和个人统计读取同一份内容数据。

本文件包含字段约定、目录规划、可直接复制的完整 JSON 示例和迁移建议。示例是格式演示及少量内容样本，不代表完整考研教材、课程目录或已上线数据。

### 设计模块与数据对应

| 设计模块 | 读取的数据 | 主要展示内容 |
| --- | --- | --- |
| 学习首页 | 目标、当日事件、最近阅读位置 | 今日进度、学习入口、接着上次学 |
| 单词 | 单词内容、单词学习记录、收藏 | 释义、发音、例句、词根、助记、熟悉程度 |
| 数学（tab） | 学科目录、章节、知识点 | 高数 / 线代 / 概率的分类、章节卡片、知识点列表、搜索 |
| 资料库（tab） | 学科目录、章节、知识点、空状态文案、政治题库 | 政治（刷题）+ 四门计算机专业课的分类与列表 |
| 知识点详情 | 知识点内容、阅读记录、计划 | 概述、关键公式或概念抓手、理解要点、讲解例子 |
| 学习计划 | 计划项及其关联知识点 | 加入计划、继续阅读、完成与移除 |
| 我的 | 学习事件、阅读记录、单词记录、目标 | 累计学习量、每周统计、收藏、目标设置 |

数学与计算机专业课均为**讲解内容**，**不设计题目、选项、正确答案、评分或错题字段**。讲解中的例子直接说明概念，不要求用户作答。

> **例外：政治**。政治目前没有可发布的知识点讲解，手上只有题目，因此政治**不做知识点列表、改做刷题**，
> 其题库是**独立于知识点格式**的另一套结构（见第 5 节「政治题库格式」）。
> 也就是说：**知识点格式**里永远没有题目字段；题目只存在于政治题库这一套结构里。
> 等政治补上真实讲解稿（`POLITICS_CHAPTERS` 非空），它会自动回到知识点列表（见 4.3）。

### 两个 tab 如何共用一套内容

设计稿里「数学」与「资料库」是两个 tab，但渲染的是同一个知识库页面，
差别只有学科清单：

| tab | 学科清单 | 学科切换器 | 页面顶部小标题 |
| --- | --- | --- | --- |
| 数学 | `calculus`、`algebra`、`probability` | 胶囊 tab（`.src-switch` / `.src-chip`），标签用全称 | 数学 · 高等数学 |
| 资料库 | `politics`、`cs-coa`、`cs-os`、`cs-ds`、`cs-net` | 下划线 tab（`.subject-tabs`），标签用简称 | 资料库 · 思想政治理论 |

清单**按 `courseId` 派生**，不写死在页面里：`courseId === 'math'` 归数学 tab，
其余归资料库 tab。这样以后往资料库里加新学科不用改页面代码，只改 `catalog.json`。

**两套切换样式按 tab 分流**（v1.6）：

- **数学（3 项）→ 胶囊 tab**：与政治卷的来源切换同款，选中项是 `primary` 实底。
  只有三项、每项是四个字的全称，横向放得下。
- **资料库（5 项）→ 下划线 tab**：5 个学科里最长的「数据结构」有 4 个字，
  再套胶囊会挤成碎块；下划线方案超 4 项时字号自动降一级以保证不换行。
  > 应用侧例外：资料库那 5 个学科落的是「白底描边胶囊」（`.subject-tab`），
  > **不是**这里写的下划线 tab。这是早于 v1.6 的既有形态，也**未登记**进样式检查脚本，
  > 因此不上表、不被守——详见文首「一处已知偏离」。要统一形态时以设计稿为准。

两套外观都由 `knowledgePage()` 里的 `subjectSwitch(tab, list)` 产出，
按钮属性完全一致（`data-action="subject" data-value="<清单内下标>"`），
只有 class 与按钮文字不同，因此 `handleAction('subject', …)` 无需区分 tab。

## 2. 内容层级与文件目录

按「课程 → 学科 → 章节 → 小节 → 知识点」组织。

- **课程**：考研英语、考研数学、考研政治、计算机专业课。
- **数学 tab 学科**：高等数学、线性代数、概率论（v1.6 由「概率统计」改名，与设计稿对齐）。
- **资料库 tab 学科**：思想政治理论、计算机组成原理、操作系统、数据结构、计算机网络。
- 英语单词保留独立入口，不加入任何学科标签。
- 政治**跟着设计稿**：原型里 `chapter` 是一个描述性字符串（`政治基础框架`）、`topics` 是
  平铺的三项，没有「章 → 节 → 知识点」三级。因此落地为 **1 章（`政治基础框架`）× 3 知识点**
  （马克思主义基本原理 / 毛泽东思想和中国特色社会主义理论体系 / 思想道德与法治）。
  > 历史偏差：早期版本把这三项当成**三个章节**、每章 1 个知识点，既不满足设计稿、
  > 也不满足本节原意。2026-09-16 起统一为跟随原型。
- 计算机专业课目前**只有学科骨架，没有章节与知识点**，页面显示空状态，不伪造内容数量。

```text
data/
  content/
    catalog.json                          课程 / 学科 / 章节
    knowledge/<subjectId>/<chapterId>.json 知识点（按章节打包）
    vocabulary/words.json                  词库（整体打包）
    assets/
      images/
      audio/
  generated/
    knowledge-index.json                   知识点搜索索引
    vocabulary-index.json                  词库搜索索引
    app/                                   小程序运行数据（见第 10 节）
```

个人记录不放入公共内容目录。小程序本地版存入本地存储；接入账号后按用户存入数据库，
两种实现使用相同业务字段。

**按章节打包而不是一个知识点一个文件**：单章知识点数量在 1～55 之间
（全库 36 章共 963 个知识点，241 个小节），一章一文件便于整章录入与校验，
也避免近千个碎文件。内容特别长的单章再考虑拆分。

`generated/` 下的索引与运行数据由内容文件生成，不手工重复维护标题、摘要和标签。

## 3. 通用约定

| 规则 | 约定 |
| --- | --- |
| 编码 | UTF-8，严格 JSON，不含注释和尾逗号 |
| 字段命名 | 小驼峰，如 `chapterId`、`updatedAt` |
| 实体 ID | 小写英文、数字和连字符；创建后保持不变，不能使用数组下标 |
| ID 示例 | `kp-m1-p1-c1-x-x0-shi-han-shu-ji-xian`、`word-persist`、`cs-coa` |
| `schemaVersion` | 数据结构版本，本文均为 `1.0` |
| `contentVersion` | 单个内容的修订号，正整数；正文修改后递增 |
| 时间 | 时间点用 ISO 8601 UTC，例如 `2026-09-15T02:30:00Z` |
| 本地日期 | 日统计使用 `YYYY-MM-DD`，按用户配置的时区计算 |
| 内容状态 | `draft` 草稿、`published` 已发布、`archived` 已归档 |
| 展示排序 | 按 `sortOrder` 升序，同值时按 `id` 稳定排序 |
| 缺省值 | 可选字段可省略；已知为空的列表用 `[]`；尚未发生的时间用 `null` |
| 数据关联 | 通过 ID 关联，改标题或调整顺序不影响阅读记录和计划 |

`schemaVersion` 描述的是**结构版本**，与文档版本号（v1.1）不是一回事：
本次新增 `kind` / `sectionId` / `module` 都是纯增字段，旧数据补上即可，因此仍为 `1.0`；
只有出现不兼容的结构变更（改字段名、改嵌套层级）才升到 `2.0`。

归档内容保留 ID，历史计划可以显示「内容已归档」，避免直接删文件导致记录失去关联。

### 富文本约定

- `summary`、标题、标签都是纯文本，用于列表与搜索。
- 以 `Markdown` 结尾的字段支持段落、列表、强调和链接，不存页面 CSS 或可执行 HTML。
- 行内公式在 Markdown 中写为 `$...$`，独立公式写为 `$$...$$`；公式卡片的 `content` 直接存 LaTeX，不加美元符号。
- JSON 中反斜杠必须转义。例如 JSON 字符串 `"\\lim"` 解析后才是 LaTeX 的 `\lim`。
- 正文换行在 JSON 字符串中写 `\n`。前端需配套 Markdown 与公式渲染能力；纯文本渲染器不会自动显示排版后的公式。
- **`anchor.format` 的实际取值**：当前 963 条数据全为 `text`，公式也用 Unicode 上标、下标、
  `→`、`∈`、`∑` 等字符书写（如 `limₓ→₀ sin x/x = 1`）而非 LaTeX 源码。
  渲染器支持 LaTeX 后可在单条内容上改用 `format: "latex"`，两种写法可共存，
  前端按 `format` 分支处理即可，不需要一次性全量迁移。
- 图片、音频如有需要，使用项目资源路径或受控资源 URL；知识内容中不保存整段 Base64 文件。

## 4. 目录数据：catalog.json

目录只描述分类、名称与顺序。章节已学数量和学习进度不写在目录里。

```json
{
  "schemaVersion": "1.0",
  "courses": [
    { "id": "english",  "name": "考研英语",     "sortOrder": 10 },
    { "id": "math",     "name": "考研数学",     "sortOrder": 20 },
    { "id": "politics", "name": "考研政治",     "sortOrder": 30 },
    { "id": "cs",       "name": "计算机专业课", "sortOrder": 40 }
  ],
  "subjects": [
    { "id": "calculus",    "courseId": "math",     "kind": "math",     "name": "高等数学",       "shortName": "高数",       "sortOrder": 10 },
    { "id": "algebra",     "courseId": "math",     "kind": "math",     "name": "线性代数",       "shortName": "线代",       "sortOrder": 20 },
    { "id": "probability", "courseId": "math",     "kind": "math",     "name": "概率论",         "shortName": "概率",       "sortOrder": 30 },
    { "id": "politics",    "courseId": "politics", "kind": "politics", "name": "思想政治理论",   "shortName": "政治",       "sortOrder": 40 },
    { "id": "cs-coa",      "courseId": "cs",       "kind": "cs",       "name": "计算机组成原理", "shortName": "组成原理",   "sortOrder": 50 },
    { "id": "cs-os",       "courseId": "cs",       "kind": "cs",       "name": "操作系统",       "shortName": "操作系统",   "sortOrder": 60 },
    { "id": "cs-ds",       "courseId": "cs",       "kind": "cs",       "name": "数据结构",       "shortName": "数据结构",   "sortOrder": 70 },
    { "id": "cs-net",      "courseId": "cs",       "kind": "cs",       "name": "计算机网络",     "shortName": "计算机网络", "sortOrder": 80 }
  ],
  "chapters": [
    {
      "id": "m1-p1-c1",
      "subjectId": "calculus",
      "module": "数学一",
      "modules": ["数学一", "数学二"],
      "title": "函数、极限与连续",
      "summary": "本章共 9 节：函数、数列极限、函数极限、无穷小与无穷大、两个重要极限与常见求极限方法 等。",
      "sortOrder": 10
    },
    {
      "id": "m2-p1-c11",
      "subjectId": "calculus",
      "module": "数学二",
      "modules": ["数学二"],
      "title": "二重积分",
      "summary": "本章共 5 节：二重积分概念、直角坐标计算、极坐标计算、对称性、应用。",
      "sortOrder": 110
    },
    {
      "id": "ch-politics-framework",
      "subjectId": "politics",
      "module": "思想政治理论",
      "title": "政治基础框架",
      "summary": "从基本原理、理论体系到思想道德与法治，把政治的三块内容串成一条线。",
      "sortOrder": 10
    }
  ]
}
```

### 字段说明

| 字段 | 层级 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- | --- |
| `id` | course / subject / chapter | string | 是 | 唯一 ID，创建后不变 |
| `name` | course / subject | string | 是 | 完整名称 |
| `title` | chapter | string | 是 | 章节标题 |
| `shortName` | subject | string | 是 | **资料库**学科切换器上的短标签，**建议不超过 5 个字**。数学 tab 用 `name` 全称（v1.6），`shortName` 留着不删——它仍是学科的唯一短名，别处要用直接可用。 |
| `courseId` | subject | string | 是 | 所属课程，必须是已存在的 course id |
| `subjectId` | chapter | string | 是 | 所属学科，必须是已存在的 subject id |
| `kind` | subject | string | 是 | `math` / `politics` / `cs`，决定插图与文案 |
| `module` | chapter | string | 是 | 章节**主要归属**的考纲范围，如 `数学一`、`思想政治理论`。列表里显示在章节标题上方 |
| `modules` | chapter | string[] | 否 | 本章**适用的全部**考纲范围。数学一与数学二共用同一套章节时只存一份，靠它标注适用范围；只有一个模块时也写出来。非数学章节可省略。前端据此做「我考数学几」筛选 |
| `summary` | chapter | string | 是 | 一句话概述 |
| `sortOrder` | 全部 | integer | 是 | 同级内展示顺序，建议 10 起步留插入空间 |

`courses`、`subjects`、`chapters` 中的上述字段均为必填。父级 ID 必须存在。

### 关于 `kind`

`kind` 是**学科的类型**，不是学科本身。设计稿里它只驱动两件事：

| 用途 | `math` | `politics` | `cs` |
| --- | --- | --- | --- |
| 章节卡片插图 | 函数曲线 | 人民大会堂 | 函数曲线（沿用） |
| 详情页抓手标题 | 关键公式 | 概念抓手 | 关键公式（沿用） |
| 详情页例子标题 | 放进例子里理解 | 放进生活里理解 | 放进例子里理解 |

**约定**：前端按 `kind` 判定，不要按 `subjectId` 硬编码。
`cs` 目前复用 `math` 的文案与插图；将来若为计算机专业课设计专属插图或文案，
在这里加一个分支即可，不需要改内容数据。

> 落地提示：`kind` 是学科属性，应从 `catalog.json` 一路投影到运行数据。
> 只保留一个「政治 → 概念抓手」的例外判断会让新增学科都要改前端。

### 没有章节的学科

`subjects` 里可以有**没有任何 `chapters` 的学科**（当前是四门计算机专业课）。
这是合法状态，表示「学科已建、内容待录入」，前端显示空状态：

- 学科标签照常显示、可切换；
- 内容区显示学科名 + 课程大纲（一句话方向）+「章节与知识点讲解正在整理中」；
- 进度显示为「暂无内容」，**不要**显示 `0 / 0` 或百分比；
- 搜索不会命中这类学科（没有已发布知识点）。

空状态文案里的大纲方向由内容侧提供，不写在前端代码里。

### 没有知识点、只有题目的学科（政治）

**政治是特例**：暂时没有可发布的知识点讲解，手上只有题目（选择题 + 材料题，含答案）。
因此政治模块**不做知识点列表，改做刷题**，数据落在 `data/politics/questions.ts`。

判断依据不是硬编码学科 id，而是两条同时满足：

1. 学科 `kind === 'politics'`；
2. 该学科在 catalog 里**没有任何章节**（`POLITICS_CHAPTERS` 当前是空数组）。

前端封装成 `useContent.isQuizSubject(subjectId)`。这样等政治补上真实讲解稿
（`data/politics/chapters.ts` 非空）时，它会**自动回到知识点列表**，不用改页面。

- 资料库里选中政治 → **直接在学科 tab 下面渲染政治内容**（而不是知识点列表，也不是一张跳转卡）。
  这一屏是**两级结构的上一级**：只画「来源切换 + 每套卷的进度卡」，没有题干；
- 刷题页在 `pages-politics` 分包（见第 10 节）；主包只放套卷投影（`politics-index.ts`）；
- **不要做一个「进入政治刷题」的独立入口页**——刷题内容本身就是政治模块的内容；
- 四门计算机专业课仍是「没有章节的学科」，走上面的空状态，**两者文案不同**：
  计算机课是「章节正在整理」，政治是「题库正在整理」。

## 5. 数学 / 政治 / 计算机专业课共用的知识点格式

知识点文件**按章节打包**，文件结构：

```text
{
  "schemaVersion": "1.0",
  "chapterId": "m1-p1-c1",
  "subjectId": "calculus",
  "module": "数学一",
  "title": "函数、极限与连续",
  "sortOrder": 10,
  "knowledge": [ ... ]        ← 知识点对象数组，每项见下方字段说明
}
```

文件头的 `chapterId` / `subjectId` / `module` / `title` / `sortOrder` 必须与
`catalog.json` 中同一章节完全一致（`tools/validate-content.ts` 会校验）。

### 知识点字段说明

| 字段 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `schemaVersion` | string | 是 | 数据结构版本 |
| `id` | string | 是 | 知识点唯一 ID |
| `contentVersion` | integer | 是 | 内容修订号 |
| `subjectId` | string | 是 | 关联学科 |
| `chapterId` | string | 是 | 关联章节，章节必须属于该学科 |
| `sectionId` | string | 是 | 所属小节 |
| `sectionTitle` | string | 是 | 小节标题，用于面包屑与列表分组 |
| `title` | string | 是 | 知识点标题，列表及详情主标题 |
| `summary` | string | 是 | 一句话概述 |
| `tags` | string[] | 是 | 分类辅助与搜索关键词，可为空 |
| `sortOrder` | integer | 是 | 章节内展示顺序 |
| `estimatedMinutes` | integer | 是 | 预计阅读分钟数，必须大于 0 |
| `anchor` | object | 是 | 关键公式或概念抓手 |
| `keyPoints` | object[] | 是 | 理解要点，**当前恒 3 项** |
| `examples` | object[] | 是 | 讲解例子，**当前恒 1 项** |
| `bodyMarkdown` | string | 是 | 完整讲解正文，供详情页展开与搜索索引使用 |
| `relatedKnowledgeIds` | string[] | 是 | 相关知识点 ID，可为空 |
| `sources` | object[] | 是 | 内容来源，可为空，不编造出处 |
| `status` | string | 是 | `draft` / `published` / `archived` |
| `updatedAt` | string | 是 | 最近修订时间 |

嵌套字段约定：

- `anchor`：`type` 为 `formula` 或 `concept`；`format` 对应 `latex` 或 `text`；
  `content` 为公式或概念文字；`caption` 为说明。四个字段均必填。
  `math` 学科用 `type: "formula"`，`politics` 用 `type: "concept"`。
- `keyPoints[]`：`id`、`title`、`bodyMarkdown` 均必填。`id` 在本知识点内唯一且稳定。
- `examples[]`：`id`、`title`、`bodyMarkdown` 均必填。不包含题干、作答或判分结构。
- `sources[]`：`title` 必填；`url`、`edition`、`locator`、`accessedAt` 可选，
  分别描述链接、版本、章节或页码、查阅日期。资料出处用于内容追溯。

### 小节与 `sectionId`

设计稿展示「正在阅读 · 第一章 / 已读 N / M 节」，需要章节下的**小节**这一层。

**约定**：`sectionId` 为 `{chapterId}-s{N}`，`{N}` 从 1 开始；
同一 `chapterId` 下的 `N` 连续、不跳号。`sectionTitle` 是该小节的中文标题。

因为 `sectionId` 可以完全由 `chapterId` 和序号推导，生成搜索索引与运行数据时可以
**不单独存整串**，只留序号即可；但规范数据里存完整 `sectionId`，便于人工核对。
若某章的小节不是自然序列（例如插入了一节又调整顺序），必须重新编号而不是跳号。

### 完整实例 A：数学知识点

文件建议：`data/content/knowledge/calculus/m1-p1-c1.json`

```json
{
  "schemaVersion": "1.0",
  "chapterId": "m1-p1-c1",
  "subjectId": "calculus",
  "module": "数学一",
  "title": "函数、极限与连续",
  "sortOrder": 10,
  "knowledge": [
    {
      "schemaVersion": "1.0",
      "id": "kp-m1-p1-c1-x-x0-shi-han-shu-ji-xian",
      "contentVersion": 1,
      "subjectId": "calculus",
      "chapterId": "m1-p1-c1",
      "sectionId": "m1-p1-c1-s3",
      "sectionTitle": "函数极限",
      "title": "函数极限的概念",
      "summary": "理解自变量趋近某一点时，函数值无限接近某个确定值的含义。",
      "tags": ["极限", "函数", "无限接近"],
      "sortOrder": 10,
      "estimatedMinutes": 5,
      "anchor": {
        "type": "formula",
        "format": "text",
        "content": "limₓ→ₓ₀ f(x) = A",
        "caption": "当 x 趋于 x₀ 时，f(x) 趋于 A；这里描述的是趋近过程。"
      },
      "keyPoints": [
        {
          "id": "guan-zhu-qu-jin-guo-cheng",
          "title": "关注趋近过程",
          "bodyMarkdown": "观察自变量接近目标位置时，函数值的变化趋势。极限关注邻近位置的行为。"
        },
        {
          "id": "zuo-you-qu-jin-ying-yi-zhi",
          "title": "左右趋近应一致",
          "bodyMarkdown": "对于内点的双侧极限，左极限与右极限必须存在且相等。"
        },
        {
          "id": "qu-fen-ji-xian-yu-han-shu-zhi",
          "title": "区分极限与函数值",
          "bodyMarkdown": "函数在该点可以没有定义；即使有定义，该点的函数值也未必等于极限。"
        }
      ],
      "examples": [
        {
          "id": "ke-qu-jian-duan-dian",
          "title": "用一个缺失点理解极限",
          "bodyMarkdown": "对于 $f(x)=\\frac{x^2-1}{x-1}$，当 $x\\ne1$ 时可化为 $x+1$。\n\n当 $x$ 趋于 1 时，函数值趋于 2。虽然原函数在 1 处没有定义，但极限仍然存在。"
        }
      ],
      "bodyMarkdown": "### 理解要点\n\n1. 观察自变量接近目标位置时，函数值的变化趋势。\n2. 对于内点的双侧极限，左极限与右极限必须存在且相等。\n3. 函数在该点可以没有定义；即使有定义，该点的函数值也未必等于极限。\n\n### 放进例子里理解\n\n对于 $f(x)=\\frac{x^2-1}{x-1}$，当 $x\\ne1$ 时可化为 $x+1$。\n\n当 $x$ 趋于 1 时，函数值趋于 2。虽然原函数在 1 处没有定义，但极限仍然存在。",
      "relatedKnowledgeIds": [],
      "sources": [],
      "status": "published",
      "updatedAt": "2026-09-15T00:00:00Z"
    }
  ]
}
```

### 完整实例 B：政治知识点

文件建议：`data/content/knowledge/politics/ch-politics-framework.json`

政治只有一章，三个知识点都挂在该章的**同一个小节**下（小节名 = 章名），
与设计稿「一个 `chapter` 字符串 + 平铺 `topics`」的形态一致。

```json
{
  "schemaVersion": "1.0",
  "chapterId": "ch-politics-framework",
  "subjectId": "politics",
  "module": "思想政治理论",
  "title": "政治基础框架",
  "sortOrder": 10,
  "knowledge": [
    {
      "schemaVersion": "1.0",
      "id": "kp-ch-politics-framework-ma-ke-si-zhu-yi-ji-ben-yuan-li",
      "contentVersion": 1,
      "subjectId": "politics",
      "chapterId": "ch-politics-framework",
      "sectionId": "ch-politics-framework-s1",
      "sectionTitle": "政治基础框架",
      "title": "马克思主义基本原理",
      "summary": "从实践出发形成认识，再用认识指导实践，在实践中检验和发展认识。",
      "tags": ["马克思主义基本原理", "实践", "认识论"],
      "sortOrder": 10,
      "estimatedMinutes": 4,
      "anchor": {
        "type": "concept",
        "format": "text",
        "content": "实践 → 认识 → 再实践 → 再认识",
        "caption": "这一过程不是简单重复，而是在实践中不断深化和发展。"
      },
      "keyPoints": [
        {
          "id": "shi-jian-shi-ren-shi-de-lai-yuan",
          "title": "实践是认识的基础",
          "bodyMarkdown": "实践是认识的来源、发展的动力和目的，也是检验认识真理性的唯一标准。"
        },
        {
          "id": "ren-shi-dui-shi-jian-you-fan-zuo-yong",
          "title": "认识对实践具有反作用",
          "bodyMarkdown": "认识能够指导实践。正确的认识有助于实践取得预期效果，错误的认识则可能使实践走弯路。"
        },
        {
          "id": "zai-fan-fu-zhong-shen-hua-ren-shi",
          "title": "在反复中深化认识",
          "bodyMarkdown": "面对复杂情况，需要在实践与认识的相互作用中检验判断、修正理解。"
        }
      ],
      "examples": [
        {
          "id": "tiao-zheng-xue-xi-fang-fa",
          "title": "用调整学习方法理解这一过程",
          "bodyMarkdown": "先记录一周的学习情况，发现自己在晚间阅读时更容易分心；据此调整阅读时间，再观察下一周的实际效果。\n\n这个例子帮助理解从实践中形成认识、再通过实践检验认识的过程。"
        }
      ],
      "bodyMarkdown": "### 理解要点\n\n1. 实践是认识的来源、发展的动力和目的，也是检验认识真理性的唯一标准。\n2. 认识能够指导实践。正确的认识有助于实践取得预期效果，错误的认识则可能使实践走弯路。\n3. 面对复杂情况，需要在实践与认识的相互作用中检验判断、修正理解。\n\n### 放进生活里理解\n\n先记录一周的学习情况，发现自己在晚间阅读时更容易分心；据此调整阅读时间，再观察下一周的实际效果。",
      "relatedKnowledgeIds": [],
      "sources": [],
      "status": "published",
      "updatedAt": "2026-09-15T00:00:00Z"
    }
  ]
}
```

> 上面只摘了第 1 个知识点（`马克思主义基本原理`），实际文件里 `knowledge[]` 有 3 条
> （另两个是「毛泽东思想和中国特色社会主义理论体系」「思想道德与法治」，`sortOrder` 20 / 30）。
> 字段取值与 `data/content/knowledge/politics/ch-politics-framework.json` 逐字段一致。

数学与政治的差别只在 `anchor.type` 与 `kind` 决定的文案，**字段结构完全相同**。
计算机专业课将来录入时也沿这套结构，`anchor.type` 视知识点性质选 `formula` 或 `concept`。

### 政治题库格式（与知识点并列的一套结构）

政治现在没有知识点，只有题目。题库单独一份规范数据：
`data/content/politics/questions.json`（源数据 `data/politics/questions.ts`）。

三种题型共用一个契约，靠 `type` 区分。公共字段：

| 字段 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `id` | string | 是 | 稳定 ID，格式 `q-<模块拼音>-<序号>`，**手工指定** |
| `type` | string | 是 | `choice`（单选）/ `multi`（多选）/ `material`（材料） |
| `module` | string | 是 | 考纲模块，取值须来自 `POLITICS_MODULES` |
| `difficulty` | integer | 是 | 1 易 / 2 中 / 3 难 |
| `stem` | string | 是 | 题干 |
| `explanation` | string | 是 | 答案解析 / 答题思路 |
| `tags` | string[] | 是 | 检索关键词 |

三种题型的额外字段：

- **单选题**：`options`（`{key, text}[]`，`key` 为 A/B/C/D）与 `answerKey`（**单个** key，须命中某个选项）。
- **多选题**：`options` 与 `answerKeys`（key **数组**，至少 2 项、不重复、须全部命中选项）。
  多选判分是「**完全一致**」——所选集合与 `answerKeys` 集合相等才算对，少选、多选、错选都算错，
  与考研政治的多选评分口径一致。
- **材料题**：`material`（`{title, paragraphs[]}`）与 `answerPoints`（采分点数组，渲染成有序列表）。

> **`answerKey` 与 `answerKeys` 互斥。** 单选题写了 `answerKeys`、或多选题写了 `answerKey`，
> 校验器**直接报错**（不是告警）。这一条是为了避免「答案存了、但判分读的是另一个字段」
> 这种静默错误——它不会让构建失败，只会让所有多选判成错。

```json
{
  "id": "q-mayuan-1",
  "type": "choice",
  "module": "马克思主义基本原理",
  "difficulty": 1,
  "tags": ["实践", "认识论", "单选题"],
  "stem": "辩证唯物主义认识论认为，检验认识真理性的唯一标准是（　　）",
  "options": [
    { "key": "A", "text": "多数人的意见" },
    { "key": "B", "text": "社会实践" }
  ],
  "answerKey": "B",
  "explanation": "真理是主观与客观相符合，只有把认识拿到实践中检验……"
}
```

多选题只换答案字段：

```json
{
  "id": "q-mayuan-3",
  "type": "multi",
  "module": "马克思主义基本原理",
  "difficulty": 2,
  "tags": ["唯物史观", "多选题"],
  "stem": "要「站在历史正确的一边」，是因为（　　）",
  "options": [
    { "key": "A", "text": "历史发展有其自身的客观规律" },
    { "key": "B", "text": "历史规律可以被人为改变" }
  ],
  "answerKeys": ["A", "C"],
  "explanation": "历史规律客观存在、不能改变，B 错……"
}
```

**约定**：

- **`id` 必须手工指定且唯一**，且只能含小写英文、数字与连字符（`/^[a-z0-9-]+$/`）。
  它是用户对错记录的键，**不要用数组下标**——题库会增删，下标一改用户记录就对不上了。
  命名惯例是从模块名转拼音（`马克思主义基本原理` → `q-mayuan-1`），但**校验器只检查格式，
  不强制含汉字**；拼音只是人写 id 时的习惯。
- **`module` 必须来自 `POLITICS_MODULES`。** 这 5 个是考纲模块的固定清单
  （马原 / 毛中特 / 史纲 / 思修法基 / 时政），校验器按白名单比对，
  写错一个字就报错。**刷题页只会列出「真的有题」的模块**——
  某模块下没有题就不会出现筛选按钮，避免「点得开却筛出空列表」。
- **`difficulty` 只能是 1 / 2 / 3**（易 / 中 / 难），仅用于展示标签。
- **`tags` 必填且必须是数组**（可为空数组）。用于关键词检索。
- **多选的 `answerKeys` 至少 2 项且不重复。** 只有 1 项的正确项应写成 `type: 'choice'`。
- **`answerPoints` 是采分点，不是逐字答案**，渲染成有序列表。
- **没有题目时不要编造**：`POLITICS_QUESTIONS` 空数组是合法状态，页面显示空态。
- 校验见 `tools/validate-content.ts` 的「3.5 政治题库」一节：id 唯一且只能含
  `[a-z0-9-]`、`module` 在枚举内、`difficulty` ∈ {1,2,3}、`tags` 是数组、
  单选 `answerKey` 命中选项、多选 `answerKeys` 是数组且 ≥2 项、不重复、全部命中选项、
  `answerKey` 与 `answerKeys` 互斥、选项 ≥2 项且 key 不重复、材料段落与采分点非空、
  `count` 与实际条数一致、`modules` 字段与真实题目交叉核对、
  以及 `pages-politics/questions.ts`（分包运行数据）必须存在且含每个 `q.id`。
  另外：**选择题若带了 `material` / `answerPoints` 会告警**（运行数据里会被忽略）。

## 6. 单词格式及完整实例

单词专用字段包括词形、音标、多个释义、例句、词根、助记和搭配。不要把这些字段塞进知识点结构。

`schemaVersion`、`id`、`contentVersion`、`courseId`、`word`、`pronunciations`、`senses`、`examples`、`collocations`、`tags`、`sources`、`status`、`updatedAt` 必填。`root` 与 `mnemonicMarkdown` 可选。`senses` 至少一项；其他数组可以为空。

- `pronunciations[]`：`accent` 为 `uk` 或 `us`，`ipa` 为音标，`audioUrl` 可选。
- `senses[]`：`partOfSpeech` 为词性，`meaningZh` 为中文释义。
- `examples[]`：`id`、`sentence`、`translationZh` 必填。
- `root`：`display`、`explanationMarkdown` 必填；词源不确定时省略整个对象。
- `collocations[]`：`phrase`、`meaningZh` 必填。

```json
{
  "schemaVersion": "1.0",
  "courseId": "english",
  "generatedAt": "2026-09-15T00:00:00Z",
  "count": 1,
  "words": [
    {
      "schemaVersion": "1.0",
      "id": "word-persist",
      "contentVersion": 1,
      "courseId": "english",
      "word": "persist",
      "pronunciations": [
        { "accent": "uk", "ipa": "pəˈsɪst" }
      ],
      "senses": [
        { "partOfSpeech": "v.", "meaningZh": "坚持；持续" }
      ],
      "examples": [
        {
          "id": "effort",
          "sentence": "If you persist in your efforts, you will make progress.",
          "translationZh": "如果坚持努力，你就会取得进步。"
        }
      ],
      "root": {
        "display": "per-（贯穿）+ sist（站立）",
        "explanationMarkdown": "源自拉丁语 persistere，有「持续站立、坚持」的含义。"
      },
      "mnemonicMarkdown": "把自己想成一棵站稳的树：风会来，但你依然坚持。",
      "collocations": [
        { "phrase": "persist in doing sth.", "meaningZh": "坚持做某事" }
      ],
      "tags": ["考研英语", "核心词汇", "动词"],
      "sources": [],
      "status": "published",
      "updatedAt": "2026-09-15T00:00:00Z"
    }
  ]
}
```

未提供音频文件时省略 `audioUrl`，由前端决定是否提供语音朗读。音标数据本身不代表一定存在可播放音频。

### 词表与深度内容分开存放

`data/english/words.ts` 是**自动生成的纯词表**（只含 `word` / `phonetic` / `meaning`），不适合手改。
四块深度内容（`examples` / `root` / `mnemonicMarkdown` / `collocations`）由人工撰写在
**`data/english/word-content.ts`**，按小写词形索引，构建时并入词条：

```
data/english/words.ts        词表（生成物）   ┐
                                        ├─→ build-content → data/content/vocabulary/words.json
data/english/word-content.ts 深度内容（人工） ┘                  → pages-words/words.ts（运行数据）
```

三条硬约定：

1. **写了就生效，写错就报错。** `word-content.ts` 里出现词库中不存在的词，构建脚本直接抛错
   （而不是静默忽略）——避免"写了半天发现没进产出"。
2. **词根词源不确定就省略 `root`**，不要猜。`examples` 每条要有真实、可独立成立的英文句子，
   不要为了填满界面造机械句。
3. **空字段整键省略。** 运行数据里绝大多数词条没有深度内容，逐条写 `"examples":[]` /
   `"root":null` 会白白多出约 300 KB（实测 651 KB → 968 KB）。前端按 `undefined` 处理空态。

> **当前覆盖情况**：5493 个词条里，**18 个**已有完整四块内容（设计稿原型那 18 词，
> 逐字转写、引号形态统一为 `「」`）。其余 5475 个仍为空。
> 因此单词详情页的四块对多数单词仍是空态，**后续按批补充，不要为了填满界面而编造**。
> 批次登记在 `data/english/word-content.ts` 的 `BATCHES`，覆盖率由构建脚本统计。

## 7. 个人学习数据格式

一名用户的数据导出快照，便于理解关联。实际数据库中可以拆成用户设置、阅读记录、
单词记录、计划项与学习事件等表。

```json
{
  "schemaVersion": "1.0",
  "userId": "demo-user-001",
  "timezone": "Asia/Shanghai",
  "settings": {
    "examYear": 2027,
    "englishExam": "english-1",
    "mathExam": "math-1"
  },
  "goalHistory": [
    {
      "effectiveFrom": "2026-09-15",
      "dailyWords": 50,
      "dailyKnowledgePoints": 3
    },
    {
      "effectiveFrom": "2026-09-16",
      "dailyWords": 30,
      "dailyKnowledgePoints": 2
    }
  ],
  "readingProgress": [
    {
      "knowledgeId": "kp-m1-p1-c1-x-x0-shi-han-shu-ji-xian",
      "status": "completed",
      "lastSectionId": "m1-p1-c1-s3",
      "startedAt": "2026-09-15T02:20:00Z",
      "lastOpenedAt": "2026-09-15T02:25:00Z",
      "completedAt": "2026-09-15T02:25:00Z",
      "completedContentVersion": 1
    },
    {
      "knowledgeId": "kp-ch-politics-framework-ma-ke-si-zhu-yi-ji-ben-yuan-li",
      "status": "reading",
      "lastSectionId": "ch-politics-framework-s1",
      "startedAt": "2026-09-15T02:30:00Z",
      "lastOpenedAt": "2026-09-15T02:30:00Z",
      "completedAt": null,
      "completedContentVersion": null
    }
  ],
  "wordProgress": [
    {
      "wordId": "word-persist",
      "familiarity": "fuzzy",
      "lastReviewedAt": "2026-09-15T02:10:00Z"
    }
  ],
  "favoriteWordIds": ["word-persist"],
  "planItems": [
    {
      "id": "plan-demo-001",
      "knowledgeId": "kp-ch-politics-framework-ma-ke-si-zhu-yi-ji-ben-yuan-li",
      "status": "pending",
      "sortOrder": 10,
      "addedAt": "2026-09-15T02:00:00Z",
      "completedAt": null
    }
  ],
  "quizProgress": [
    {
      "questionId": "q-mayuan-1",
      "picked": "B",
      "correct": true,
      "answeredAt": "2026-09-15T02:40:00Z"
    }
  ],
  "learningEvents": [
    {
      "id": "event-demo-001",
      "type": "word_reviewed",
      "contentId": "word-persist",
      "contentVersion": 1,
      "occurredAt": "2026-09-15T02:10:00Z",
      "localDate": "2026-09-15",
      "timezone": "Asia/Shanghai",
      "familiarity": "fuzzy"
    },
    {
      "id": "event-demo-002",
      "type": "knowledge_completed",
      "contentId": "kp-m1-p1-c1-x-x0-shi-han-shu-ji-xian",
      "contentVersion": 1,
      "occurredAt": "2026-09-15T02:25:00Z",
      "localDate": "2026-09-15",
      "timezone": "Asia/Shanghai"
    },
    {
      "id": "event-demo-003",
      "type": "quiz_answered",
      "contentId": "q-mayuan-1",
      "contentVersion": 1,
      "occurredAt": "2026-09-15T02:40:00Z",
      "localDate": "2026-09-15",
      "timezone": "Asia/Shanghai"
    }
  ],
  "updatedAt": "2026-09-15T02:30:00Z"
}
```

### 个人记录规则

- 阅读状态：`reading`、`completed`；没有记录表示未开始。`lastSectionId` 可为 `null`，
  否则须对应当前知识点所在章节的合法 `sectionId`。
- 单词熟悉程度：`unknown` 不认识、`fuzzy` 模糊、`familiar` 熟悉；未学习单词没有记录。
- 计划状态：`pending`、`completed`。每个用户对同一知识点最多一个有效计划项。
  阅读完成时同步完成计划；移除计划不删除阅读历史。
- 学习事件：`word_reviewed` 引用单词并要求 `familiarity`；`knowledge_completed` 引用知识点；
  `quiz_answered` 引用题目。内容 ID 类型要与事件类型匹配，
  **不要用同一条事件混合记录两类内容**。
- 政治刷题记录 `quizProgress[]`：`{questionId, picked, correct, answeredAt}`。
  `questionId` 是题库稳定 ID；`picked` 是所选选项 key（材料题为空串）。
  **多选题的 `picked` 是所选 key 排序后拼接的字符串**（如选 A、C 存 `"AC"`），
  这样 `picked` 保持 string 类型，单选 / 多选 / 材料题三种情况共用同一字段。
  同一题重复作答**覆盖**旧记录（保留最近一次对错），但事件只在当天首次作答时写一条，
  避免反复刷同一题把「今日完成题数」刷上去。
- 事件 ID 在创建时生成并保持不变；同步重试按同一 ID 去重，避免重复计数。
- `englishExam`：`english-1`、`english-2`；`mathExam`：`math-1`、`math-2`、`math-3`。
  它们是**个人目标设置**，不能据此自动推断知识点适用范围；考纲筛选请用章节的 `module`。
- `dailyWords`、`dailyKnowledgePoints` 为正整数；选项分别为 `[20,30,50,80]`、`[1,2,3,5]`。
- 目标变更记录保留生效日期。当天选取 `effectiveFrom` 不晚于当天的最近一条记录；
  新目标从**次日**生效，同一生效日只保留一条目标配置。
- `completedContentVersion` 保存完成时看到的版本。内容更新后可提示「讲解有更新」，
  不静默清空已读状态。
- 本地多账号使用不同的存储键，例如 `cishu:user:demo-user-001:v1`。

## 8. 搜索索引与统计口径

### 搜索索引

由已发布知识点生成轻量索引；详情需要时再加载正文。

```json
{
  "schemaVersion": "1.0",
  "generatedAt": "2026-09-15T03:00:00Z",
  "items": [
    {
      "id": "kp-m1-p1-c1-x-x0-shi-han-shu-ji-xian",
      "contentVersion": 1,
      "subjectId": "calculus",
      "chapterId": "m1-p1-c1",
      "sectionId": "m1-p1-c1-s3",
      "title": "函数极限的概念",
      "summary": "理解自变量趋近某一点时，函数值无限接近某个确定值的含义。",
      "tags": ["极限", "函数", "无限接近"],
      "sortOrder": 10,
      "estimatedMinutes": 5,
      "contentPath": "knowledge/calculus/m1-p1-c1.json",
      "searchText": "高等数学 高数 数学一 函数、极限与连续 函数极限 函数极限的概念 极限 函数 无限接近"
    }
  ]
}
```

`contentPath` 相对于 `data/content/`，指向**章节文件**（不是单个知识点）。
`searchText` 由课程、学科及简称、`module`、章节标题、`sectionTitle`、知识点标题、摘要、
标签和去掉格式后的讲解文字拼接生成；上例为便于阅读的缩短演示值，生成器应输出完整结果。

搜索时对查询与索引统一去首尾空格、转换英文大小写。设计稿的搜索框**跨全部学科**，
不限于当前 tab；无结果时提示可尝试的关键词。

### 统计规则

| 展示项 | 建议计算方法 |
| --- | --- |
| 章节总知识点 | 当前章节中 `published` 知识点数量 |
| 章节已学数量 | 当前章节中已完成的已发布知识点去重数量 |
| 章节进度 | 已学数量 ÷ 总知识点；总数为 0 时显示「暂无内容」 |
| 今日单词数 | 当日本地日期的 `word_reviewed` 事件，按单词 ID 去重 |
| 今日知识点数 | 当日本地日期的 `knowledge_completed` 事件，按知识点 ID 去重 |
| 今日待办数 | 单词目标与知识点目标中尚未达标的类别数，范围 0～2 |
| 今日进度环 | 单词达成率与知识点达成率的平均值，各自最高计 100% |
| 累计学习量 | 所有历史完成事件，按对应内容 ID 去重；可包含后续归档内容 |
| 已练题目 | `quiz_answered` 事件按题目 ID 去重（与「已学习单词」「已阅读知识点」并列） |
| 每周柱状图 | 按本地日期分组，统计每天去重内容数；没有事件的日期补 0。**三种模式：单词 / 知识点 / 刷题** |
| 连续学习天数 | 从今天向前统计有学习事件的连续日期；今天尚未学习则从昨天开始 |
| 接着上次学 | 从阅读记录取 `lastOpenedAt` 最新的内容，跳转至 `lastSectionId` |

**刷题（政治）的统计口径**：刷题**不计入「今日目标」**——
「今日待办数」仍是单词 + 知识点两项（范围 0～2），「今日进度环」仍是两项达成率的平均。
刷题在首页是**并列的第三个学习入口**（整行的「政治刷题」卡片，显示「已练 M / 共 T 题」），
在「我的」页进入累计统计与周图表的第三种模式。这样做的原因：政治没有每日目标设置项，
把它塞进目标环会让「今日待办」的含义变得含糊。

> 首页卡片需要「题库总量 T」，但题库正文在 `pages-politics` 分包内、主包不能 import。
> 因此构建时只把**总数这一个数字**投影到主包的 `data/generated/app/catalog.ts`
> （`appPoliticsQuestionTotal`），不投影题目本身。

同一内容当天重复阅读或反复复习只计一次当日数量；不同日期再次完成可计入各自当日数量，
但累计内容数只计一次。每周总量等于每日数量之和，含跨日复习，
不等于一周内不同内容的总数。

知识点总数、已读数、进度百分比、待办数量均由数据计算，**不把设计稿里的示例常量写入内容文件**。
尤其不能用累计已读数量计算「今日完成」，或用计划项数量判断今日目标是否完成。

## 9. 从当前设计稿迁移

设计稿的学科表里，每个学科把章节名写了进去（如 `chapter:'函数与极限'`），
且四门计算机专业课的 `chapter` 字段塞的是一串大纲短语。迁移时注意拆分：

| 当前设计稿字段或结构 | 建议存放方式 |
| --- | --- |
| `subjects[]` 的 `name` / `short` | `catalog.json` 的 `subjects[].name` / `shortName` |
| `subjects[]` 的 `kind` | `catalog.json` 的 `subjects[].kind` |
| `subjects[]` 的 `chapter` | 拆入 `catalog.json` 的 `chapters[].title`（见下方说明） |
| `subjects[]` 的 `intro` | 拆分：数学/政治进章节 `summary`；无章节学科作为空状态的大纲文案 |
| `subjects[]` 的 `chapter`（四门计算机课） | **不拆成章节**，仅作为空状态的课程大纲展示 |
| `MATH_SUBJECT_IDS` / `LIBRARY_SUBJECT_IDS` | 由 `subjects[].courseId` 派生，不写死在代码里 |
| `topics[]`、`notes[]`、`concepts[][]` | 合并为独立知识点对象，取消平行数组与位置关联 |
| `concepts[].formula` | 数学转 `anchor` 公式，政治转 `anchor` 概念文字 |
| `concepts[].steps` | 转为带稳定 ID 的 `keyPoints[]` |
| `concepts[].example` | 转为 `examples[]` |
| `subjects[].progress`、`total` | 分别由个人阅读记录与已发布内容计算 |
| `readTopics` | 转为 `readingProgress`；原型没有真实阅读时间时不补造历史事件 |
| `lastTopic` | 用最近打开的阅读记录和小节位置表示 |
| `planned` | 转为 `planItems` |
| `answered[word]` | 转为以稳定单词 ID 关联的 `wordProgress` |
| 数组下标形式的 `favorites` | 转为 `favoriteWordIds` |
| `goals.words`、`goals.knowledge` | 转为带生效日期的 `goalHistory` |

> **`subjects[].chapter` 要按语义拆**。数学学科里它是**真正的章节名**（`函数与极限`），
> 应成为 `chapters[].title`；四门计算机学科里它是一串用 `·` 连接的大纲短语
> （`线性表 · 树与二叉树 · 图 · 查找与排序`），**不能**当成章节标题，
> 只作为空状态展示的课程大纲。判断依据：该学科是否真的有已发布知识点。

> **不要从数学数据反推学科清单**。设计稿有 4 个学科分类（含政治），
> 而内容是数学 + 政治 + 计算机三类来源，从 `math-data` 派生会静默丢掉政治与计算机专业课。

旧 ID 如 `calculus-0` 需要建立明确映射表。政治原型的大专题拆成多个知识点时，
旧已读记录不可自动把所有子知识点标为已读；可保留旧导入记录供迁移追溯。

## 10. 运行数据投影与分包

`data/content` 是给人看、给脚本维护的规范数据，体积远超声小程序限制。
因此需要一层**投影**：只保留界面真正渲染的字段，并按微信分包边界摆放。

### 投影后的字段裁剪

| 规范字段 | 运行数据里是否保留 | 说明 |
| --- | --- | --- |
| `anchor.content` | 保留 | 详情页主展示 |
| `anchor.caption` | **必须保留** | 与 `summary` 首句**并不总一致**，不能派生 |
| `anchor.type` / `format` | 可裁 | 全库恒定，前端按 `kind` 判定即可 |
| `keyPoints[].bodyMarkdown` | 保留 | 退化成 `string[]` |
| `keyPoints[].title` | 可裁 | 详情页只渲染编号 + 正文 |
| `keyPoints[].id` | 保留 | 阅读位置定位需要 |
| `examples[0].bodyMarkdown` | 保留 | 当前恒 1 条，可直接降为字符串字段 |
| `examples[].id` / `title` | 可裁 | 界面不渲染 |
| `bodyMarkdown` | 可裁 | 详情页不展开全文时不必下发，但搜索索引要用 |

单词侧：

| 规范字段 | 运行数据里是否保留 | 说明 |
| --- | --- | --- |
| `word` / `pronunciations` / `senses` | 保留 | 展平为 `word` / `ipa` / `pos` / `meaning` |
| `examples[].sentence` / `translationZh` | 保留 | 降为 `{ sentence, translation }`，`id` 省略 |
| `root.display` / `explanationMarkdown` | 保留 | 降为 `{ display, explanation }`；无则整键省略 |
| `mnemonicMarkdown` | 保留 | 降为 `mnemonic`；无则整键省略 |
| `collocations[].phrase` / `meaningZh` | 保留 | 降为 `{ phrase, meaning }`；无则整键省略 |
| 上述四块为空时 | **整键省略** | 不写 `[]` / `null`，否则全库多出约 300 KB |

政治题库侧：

| 规范字段 | 运行数据里是否保留 | 说明 |
| --- | --- | --- |
| `id` / `type` / `module` | 保留 | `id` 是对错记录的键，绝不能裁 |
| `difficulty` / `stem` / `explanation` / `tags` | 保留 | 列表与答题页都要用 |
| `options[].key` / `text`（单选 / 多选） | 保留 | 保留成 `{key, text}`，**不要退化成纯字符串**——判对错要按 key 比对 |
| `answerKey`（单选） | 保留 | 作答判定的依据 |
| `answerKeys[]`（多选） | 保留 | 多选作答判定的依据；与 `answerKey` 互斥 |
| `material.title` / `paragraphs[]`（材料题） | 保留 | 题干材料，逐段渲染 |
| `answerPoints[]`（材料题） | 保留 | 采分点，渲染成有序列表 |
| 未用到的那一半字段 | **整键省略** | 单选 / 多选不写 `material` / `answerPoints`，材料题不写 `options` / `answerKey` / `answerKeys` |
| 顶层 `count` | 保留 | 校验器会比对它与实际条数 |
| 顶层 `modules[]` | 保留 | 只含**真的有题**的模块，供校验器交叉核对题目 |

政治套卷投影侧（`data/generated/app/politics-index.ts`，**主包**，由同一个脚本产出）：

它不再是「一条题目一行」，而是**一套卷一行**：`{ book, set, ids[] }`。
主包那一屏只画套卷卡——「共 N 题」= `ids.length`，「已练 M 题」= 用 id 去查同学作答记录——
所以除了 id 什么都不需要：

| 规范字段 | 投影里是否保留 | 说明 |
| --- | --- | --- |
| `id`（收进各套卷的 `ids[]`） | 保留 | 数题量与查作答记录都靠它 |
| `book` / `set` | 保留 | 就是这一行的键；不属于任何一卷的题不进投影 |
| `stem` | **裁掉** | 题干只在卷详情页（分包）出现；留着它等于让主包替分包背 51 KB 正文 |
| `type` / `module` / `difficulty` | **裁掉** | 只服务于已删除的列表行与模块筛选 chip |
| `tags` | **裁掉** | 只用于推导 `book` / `set`，界面不渲染 |
| `options` / `answerKey` / `answerKeys` / `explanation` | **裁掉** | 只有答题页用；答题页在分包内，直接读完整题库 |
| `material` / `answerPoints` | **裁掉** | 同上 |

> **投影里不许有题干**，这条由 `tools/check-bundles.mjs` 第 3 节守着（它同时断言
> `components/PoliticsQuiz.vue` 里不出现 `.stem`）。理由反过来也成立：
> 只要界面不在主包那一屏画题干，主包就没有任何理由带题干。

> 反过来，**分包里的题干不要按界面长度截断**。列表行只显示两行，但那是 CSS 的事：
> 数据层裁了就再也拿不回来，将来放宽显示还得重跑构建。裁字段要有「派生是否严格等价」的证据，
> 而截断题干既不等价、也没有必要。

> 与单词侧同理：**空的部分整键省略**，不写 `null` / `[]`。运行数据里的类型是
> `AppPoliticsQuestion`（定义在 `pages-politics/questions.ts`），除 `id` 与 `type` 外
> 大部分字段是**可选**的——因为同一套接口要给两种题型用。

**裁剪前必须验证派生是否严格等价**，不要凭「看起来一样」就删字段。
曾经把 `anchor.caption` 裁掉改用 `summary` 首句派生，960 条里有 61 条两者不一致，
会显示成另一段文字。

### 分包边界

| 位置 | 内容 |
| --- | --- |
| 主包 | 课程 / 学科 / 章节、知识点列表（标题 + 摘要 + 时长）、单词 id 清单、政治套卷投影（每套卷的题目 id） |
| `pages-knowledge` 分包 | 知识点正文（anchor / caption / 要点 / 例子） |
| `pages-words` 分包 | 词库全量 |
| `pages-politics` 分包 | 政治题库正文（题干 / 选项 / 答案 / 解析 / 材料 / 采分点）+ 卷详情页 + 答题页 + 刷题会话状态 |

两条硬约束：

1. **分包数据文件必须物理放在分包目录内**。放在公共数据目录下会被
   当作公共模块**打回主包**，分包只剩空页面。
2. **主包不能引用分包数据**。只要有一个主包文件 import 了它，分包就会被拉回主包。
   因此主包侧只暴露 id 清单 / 轻量清单，正文由分包页面自己解析。

政治题库遵循同样的约束：题库存放在 `pages-politics/questions.ts`，
刷题会话状态也在分包内（`pages-politics/usePoliticsQuiz.ts`）——
**不要把它挪到 `stores/`**，那会让主包引用到题库。主包只存对错记录（只有 id，见第 7 节）。

但政治模块的**内容要显示在主包的资料库里**（设计稿如此）。所以投影是必需的：
`politics-index.ts` 只放**每套卷的题目 id**，套卷卡点下去只是带上卷号
（`pool=x4-1` … `x8-8`）跳到分包里的卷详情页 / 答题页，由它们用**完整题库**解析题干、建队列。
**队列必须在分包里建**——主包那份投影连题干都没有，更判不了对错。

同理，政治学科在主包内不渲染「入口卡 + 跳转」，而是直接内嵌 `components/PoliticsQuiz.vue`
（两个宿主共用：资料库内嵌 + 结果页返回的独立刷题页）。但**内嵌的是索引级那一屏**：
来源切换 + 套卷卡。一轮练习的题目列表在 `pages-politics/paper/paper.vue`（分包内，
`pages.json` 已注册），题干只在那里出现。这是「两级结构」，不是「入口卡」——
入口卡指的是把人领到另一个页面的空卡片，而学科 tab 下本身已经是能操作的内容。

验证方法：编译后看 `unpackage/dist/dev/mp-weixin/pages-politics/` 里有没有题库 chunk，
并确认主包与 `common/` 里搜不到题目 ID 与**题干文字**。新增投影后还要确认
`components/PoliticsQuiz.vue` 只 import 主包模块（它是主包组件，一旦 import 分包数据
或渲染 `.stem`，就把题库拉回主包了）——这两条都由 `tools/check-bundles.mjs` 第 1、3 节守着。

## 11. 录入与导入检查

1. JSON 能成功解析，必填字段存在，枚举及数据类型符合本文约定。
2. 内容 ID 不重复；章节、学科、相关知识点与个人记录的关联 ID 有效。
3. 知识点文件的文件头字段与 `catalog.json` 中同一章节一致。
4. `sectionId` 在章节内连续不跳号，且与 `chapterId` 前缀匹配。
5. 每个知识点包含 3 个理解要点和至少 1 个讲解例子，小节 ID 不重复。
6. 每个学科都有 `kind`；新增学科不需要改前端代码即可出现在正确的 tab 下。
7. 数学公式通过实际渲染检查；政治概念卡使用文本格式。
8. 草稿不进入正式搜索；内容发布或归档后重新生成索引，更新正文缓存版本。
9. 进度、计划和统计按用户隔离；同步时按事件 ID 去重。
10. 原型迁移不把示例进度或缺失时间当作真实历史；上线后按实际事件累计。
11. 政治题库：`id` 唯一且只含 `[a-z0-9-]`、单选 `answerKey` 命中某个选项、
    多选 `answerKeys` 是数组且 ≥2 项、不重复、全部命中选项、`answerKey` 与 `answerKeys` 互斥、
    选项 ≥2 且 key 不重复、材料段落与采分点非空、`module` 来自 `POLITICS_MODULES`、
    `difficulty` ∈ {1,2,3}、`tags` 是数组、`count` 与实际条数一致、`modules` 与真实题目交叉核对。
12. 每个学科都有 `kind`（含政治与四门计算机专业课）——**缺了 `kind` 政治就落不到刷题页**。
13. 政治题库运行数据写在**分包内部**（`pages-politics/questions.ts`），
    且每个 `q.id` 都能在该文件里找到——放 `data/generated/` 会被打回主包。
14. 同一页面按学科分流内容时，**不能整页提前 return**：标题与学科切换器要永远渲染，
    只在内容容器里换内容。否则选中政治会把学科切换器一起吞掉，用户切不走。
15. 主包的**套卷投影**（`data/generated/app/politics-index.ts`）与分包题库**逐条对齐**：
    每套卷的 `ids` 与题库里该套卷的 id 完全一致（**含卷面顺序**）、不漏卷、不串卷，
    不属于任何一卷的题不得被塞进卷里；投影里只有 id（没有 `stem` / `options` / `answerPoints`）。
    主包任何文件都不得 import `pages-politics/questions.ts`——
    资料库内嵌的 `components/PoliticsQuiz.vue` 是主包组件，最容易踩这条。
16. **政治是两级**：学科 tab 下只画来源切换 + 套卷卡，一套卷里的题目列表与题干在
    `pages-politics/paper/paper.vue`（分包，已在 `pages.json` 注册）。
    **主包组件里不得出现 `.stem`**，投影里也不得有题干——判据是「主包体积不随题库正文变化」，
    两处任一处写上题干，主包就会跟着题库一起涨（曾经涨到 202 KB）。
    第 15、16 两条由 `node tools/check-bundles.mjs` 一站式跑出来。
17. **学科切换器按 tab 分两套外观**：数学（3 项）用胶囊 tab（`.src-switch` / `.src-chip`，
    与政治卷来源切换同款），标签取 `name` 全称；资料库（5 项）用下划线 tab
    （`.subject-tabs`），标签取 `shortName` 简称。两套外观的按钮属性必须一致
    （`data-action="subject"` + `data-value` = 清单内下标），点击处理不得区分 tab。
    **断言学科切换器时要锚 `data-od-id="subject-switch"`，不要按 class 找**：
    政治页里本来就有个 `.src-switch`（题库的来源切换），按 class 找会误判。
    该条由 `node tools/_library-tabs-harness.mjs` 与 `node tools/_od-harness.mjs` 覆盖。

建议先按目录、知识点、单词、个人记录这四类数据落地。后续扩充图片、音频或更多章节时，
通过明确增加字段和结构版本演进，保持现有 ID 与个人记录关联稳定。
