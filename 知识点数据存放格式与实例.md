# 词数同行：知识点数据存放格式与实例

版本：1.0（建议方案）  
日期：2026-09-15  
依据：更新后的「词数同行小程序」设计稿，入口文件 `ci-shu-tong-xing.html`。

## 1. 推荐方案

采用 **JSON 存结构化数据，Markdown 存讲解正文，LaTeX 存数学公式**。Markdown 正文和 LaTeX 公式直接放在 JSON 字符串字段里，第一版无需为每个知识点另建正文文件。

数学和政治使用统一的知识点格式；单词使用独立格式。公共内容与个人记录分别保存，用稳定 ID 关联。学习首页、资料库、搜索和个人统计读取同一份内容数据。

本文件包含字段约定、目录规划、可直接复制的完整 JSON 示例和迁移建议。示例是格式演示及少量内容样本，不代表完整考研教材、课程目录或已上线数据。本文仅规划数据，不修改现有应用。

### 设计模块与数据对应

| 设计模块 | 读取的数据 | 主要展示内容 |
| --- | --- | --- |
| 学习首页 | 目标、当日事件、最近阅读位置 | 今日进度、学习入口、接着上次学 |
| 单词 | 单词内容、单词学习记录、收藏 | 释义、发音、例句、词根、助记、熟悉程度 |
| 资料库：高数 / 线代 / 概率 / 政治 | 学科目录、章节、知识点 | 分类、章节卡片、知识点列表、搜索 |
| 知识点详情 | 知识点内容、阅读记录、计划 | 概述、关键公式或概念、理解要点、讲解例子 |
| 学习计划 | 计划项及其关联知识点 | 加入计划、继续阅读、完成与移除 |
| 我的 | 学习事件、阅读记录、单词记录、目标 | 累计学习量、每周统计、收藏、目标设置 |

数学与政治均为讲解内容，不设计题目、选项、正确答案、评分或错题字段。讲解中的例子直接说明概念，不要求用户作答。

## 2. 内容层级与文件目录

建议按「课程 → 学科 → 章节 → 知识点」组织。

- 课程：英语、数学、政治。
- 资料库学科标签：高等数学、线性代数、概率统计、思想政治理论。
- 英语单词保留独立入口，不加入资料库的四个学科标签。
- 政治设计稿中的「马克思主义基本原理」「毛泽东思想和中国特色社会主义理论体系」「思想道德与法治」建议作为章节或专题，再拆成可单独阅读的知识点。例如「实践与认识的关系」。这是对原型内容粒度的建议调整。

建议的后续落地目录如下；目录树是规划，不表示这些文件已创建：

```text
data/
  content/
    catalog.json
    knowledge/
      calculus/kp-calculus-limit-concept.json
      politics/kp-politics-practice-cognition.json
    vocabulary/
      word-persist.json
    assets/
      images/
      audio/
  generated/
    knowledge-index.json
    vocabulary-index.json
```

个人记录不放入公共内容目录。小程序本地版可存入本地存储；接入账号后，可按用户存入数据库。两种实现使用相同业务字段。

一份知识点文件对应一个可阅读单元；内容较多时再按章节打包。`generated/` 下的索引由内容文件生成，不手工重复维护标题、摘要和标签。

## 3. 通用约定

| 规则 | 约定 |
| --- | --- |
| 编码 | UTF-8，严格 JSON，不含注释和尾逗号 |
| 字段命名 | 小驼峰，如 `chapterId`、`updatedAt` |
| 实体 ID | 小写英文、数字和连字符；创建后保持不变，不能使用数组下标 |
| ID 示例 | `kp-calculus-limit-concept`、`word-persist` |
| `schemaVersion` | 数据结构版本，本文均为 `1.0` |
| `contentVersion` | 单个内容的修订号，正整数；正文修改后递增 |
| 时间 | 时间点用 ISO 8601 UTC，例如 `2026-09-15T02:30:00Z` |
| 本地日期 | 日统计使用 `YYYY-MM-DD`，按用户配置的时区计算 |
| 内容状态 | `draft` 草稿、`published` 已发布、`archived` 已归档 |
| 展示排序 | 按 `sortOrder` 升序，同值时按 `id` 稳定排序 |
| 缺省值 | 可选字段可省略；已知为空的列表用 `[]`；尚未发生的时间用 `null` |
| 数据关联 | 通过 ID 关联，改标题或调整顺序不影响阅读记录和计划 |

归档内容保留 ID，历史计划可以显示“内容已归档”，避免直接删文件导致记录失去关联。

### 富文本约定

- `summary`、标题、标签都是纯文本，用于列表与搜索。
- 以 `Markdown` 结尾的字段支持段落、列表、强调和链接，不存页面 CSS 或可执行 HTML。
- 行内公式在 Markdown 中写为 `$...$`，独立公式写为 `$$...$$`；公式卡片的 `content` 直接存 LaTeX，不加美元符号。
- JSON 中反斜杠必须转义。例如 JSON 字符串 `"\\lim"` 解析后才是 LaTeX 的 `\lim`。
- 正文换行在 JSON 字符串中写 `\n`。前端需配套 Markdown 与公式渲染能力；纯文本渲染器不会自动显示排版后的公式。
- 图片、音频如有需要，使用项目资源路径或受控资源 URL；知识内容中不保存整段 Base64 文件。

## 4. 目录数据：catalog.json

目录只描述分类、名称与顺序。章节已学数量和学习进度不写在目录里。

以下是最小目录示例：完整列出四个资料库学科；章节只列本文件用到的部分，实际录入时继续扩充。

```json
{
  "schemaVersion": "1.0",
  "courses": [
    { "id": "english", "name": "考研英语", "sortOrder": 10 },
    { "id": "math", "name": "考研数学", "sortOrder": 20 },
    { "id": "politics", "name": "考研政治", "sortOrder": 30 }
  ],
  "subjects": [
    { "id": "calculus", "courseId": "math", "name": "高等数学", "shortName": "高数", "sortOrder": 10 },
    { "id": "algebra", "courseId": "math", "name": "线性代数", "shortName": "线代", "sortOrder": 20 },
    { "id": "probability", "courseId": "math", "name": "概率统计", "shortName": "概率", "sortOrder": 30 },
    { "id": "politics", "courseId": "politics", "name": "思想政治理论", "shortName": "政治", "sortOrder": 40 }
  ],
  "chapters": [
    {
      "id": "ch-calculus-limits",
      "subjectId": "calculus",
      "title": "函数与极限",
      "summary": "从直觉出发，理解变化与无限接近。",
      "sortOrder": 10
    },
    {
      "id": "ch-politics-marxism",
      "subjectId": "politics",
      "title": "马克思主义基本原理",
      "summary": "理解实践、认识及其相互关系。",
      "sortOrder": 10
    },
    {
      "id": "ch-politics-theory",
      "subjectId": "politics",
      "title": "毛泽东思想和中国特色社会主义理论体系",
      "summary": "结合历史与实践理解理论的发展。",
      "sortOrder": 20
    },
    {
      "id": "ch-politics-ethics-law",
      "subjectId": "politics",
      "title": "思想道德与法治",
      "summary": "理解理想信念、道德实践与法治意识。",
      "sortOrder": 30
    }
  ]
}
```

`courses`、`subjects`、`chapters` 中的字段均为必填。父级 ID 必须存在。没有已发布知识点的章节显示空状态，不伪造内容数量。

## 5. 数学与政治共用的知识点格式

### 字段说明

| 字段 | 类型 | 必填 | 用途 |
| --- | --- | --- | --- |
| `schemaVersion` | string | 是 | 数据结构版本 |
| `id` | string | 是 | 知识点唯一 ID |
| `contentVersion` | integer | 是 | 内容修订号 |
| `subjectId` | string | 是 | 关联资料库学科 |
| `chapterId` | string | 是 | 关联章节，章节必须属于该学科 |
| `title` | string | 是 | 列表及详情标题 |
| `summary` | string | 是 | 一句话概述，建议 30～80 字 |
| `tags` | string[] | 是 | 分类辅助与搜索关键词，可为空 |
| `sortOrder` | integer | 是 | 章节内展示顺序 |
| `estimatedMinutes` | integer | 是 | 预计阅读分钟数，必须大于 0 |
| `anchor` | object | 是 | 关键公式或概念抓手 |
| `keyPoints` | object[] | 是 | 理解要点；当前设计要求 3 项 |
| `examples` | object[] | 是 | 至少 1 个讲解例子 |
| `bodyMarkdown` | string | 否 | 需要时补充较长讲解 |
| `relatedKnowledgeIds` | string[] | 是 | 相关知识点 ID，可为空 |
| `sources` | object[] | 是 | 内容来源，可为空，不编造出处 |
| `status` | string | 是 | `draft` / `published` / `archived` |
| `updatedAt` | string | 是 | 最近修订时间 |

嵌套字段约定：

- `anchor`：`type` 为 `formula` 或 `concept`；`format` 对应 `latex` 或 `text`；`content` 为公式或概念文字；`caption` 为说明。四个字段均必填。
- `keyPoints[]`：`id`、`title`、`bodyMarkdown` 均必填。`id` 在本知识点内唯一且稳定，便于保存阅读位置。
- `examples[]`：`id`、`title`、`bodyMarkdown` 均必填。不包含题干、作答或判分结构。
- `sources[]`：`title` 必填；`url`、`edition`、`locator`、`accessedAt` 可选，分别描述链接、版本、章节或页码、查阅日期。资料出处用于内容追溯。

### 完整实例 A：数学知识点

文件建议：`data/content/knowledge/calculus/kp-calculus-limit-concept.json`

```json
{
  "schemaVersion": "1.0",
  "id": "kp-calculus-limit-concept",
  "contentVersion": 1,
  "subjectId": "calculus",
  "chapterId": "ch-calculus-limits",
  "title": "函数极限的概念",
  "summary": "理解自变量趋近某一点时，函数值无限接近某个确定值的含义。",
  "tags": ["极限", "函数", "无限接近"],
  "sortOrder": 10,
  "estimatedMinutes": 5,
  "anchor": {
    "type": "formula",
    "format": "latex",
    "content": "\\lim_{x \\to x_0} f(x) = A",
    "caption": "当 x 趋于 x₀ 时，f(x) 趋于 A；这里描述的是趋近过程。"
  },
  "keyPoints": [
    {
      "id": "approach",
      "title": "关注趋近过程",
      "bodyMarkdown": "观察自变量接近目标位置时，函数值的变化趋势。极限关注邻近位置的行为。"
    },
    {
      "id": "two-sides",
      "title": "左右趋近应一致",
      "bodyMarkdown": "对于内点的双侧极限，左极限与右极限必须存在且相等。"
    },
    {
      "id": "point-value",
      "title": "区分极限与函数值",
      "bodyMarkdown": "函数在该点可以没有定义；即使有定义，该点的函数值也未必等于极限。"
    }
  ],
  "examples": [
    {
      "id": "removable-gap",
      "title": "用一个缺失点理解极限",
      "bodyMarkdown": "对于 $f(x)=\\frac{x^2-1}{x-1}$，当 $x\\ne1$ 时可化为 $x+1$。\n\n当 $x$ 趋于 1 时，函数值趋于 2。虽然原函数在 1 处没有定义，但极限仍然存在。"
    }
  ],
  "relatedKnowledgeIds": [],
  "sources": [],
  "status": "draft",
  "updatedAt": "2026-09-15T00:00:00Z"
}
```

### 完整实例 B：政治知识点

文件建议：`data/content/knowledge/politics/kp-politics-practice-cognition.json`

```json
{
  "schemaVersion": "1.0",
  "id": "kp-politics-practice-cognition",
  "contentVersion": 1,
  "subjectId": "politics",
  "chapterId": "ch-politics-marxism",
  "title": "实践与认识的关系",
  "summary": "从实践出发形成认识，再用认识指导实践，在实践中检验和发展认识。",
  "tags": ["马克思主义基本原理", "实践", "认识论"],
  "sortOrder": 10,
  "estimatedMinutes": 4,
  "anchor": {
    "type": "concept",
    "format": "text",
    "content": "实践 → 认识 → 再实践 → 再认识",
    "caption": "这一过程不是简单重复，而是在实践中不断深化和发展。"
  },
  "keyPoints": [
    {
      "id": "practice-foundation",
      "title": "实践是认识的基础",
      "bodyMarkdown": "实践是认识的来源、发展的动力和目的，也是检验认识真理性的唯一标准。"
    },
    {
      "id": "cognition-guidance",
      "title": "认识对实践具有反作用",
      "bodyMarkdown": "认识能够指导实践。正确的认识有助于实践取得预期效果，错误的认识则可能使实践走弯路。"
    },
    {
      "id": "continuous-development",
      "title": "在反复中深化认识",
      "bodyMarkdown": "面对复杂情况，需要在实践与认识的相互作用中检验判断、修正理解。"
    }
  ],
  "examples": [
    {
      "id": "study-routine",
      "title": "用调整学习方法理解这一过程",
      "bodyMarkdown": "先记录一周的学习情况，发现自己在晚间阅读时更容易分心；据此调整阅读时间，再观察下一周的实际效果。\n\n这个例子帮助理解从实践中形成认识、再通过实践检验认识的过程。"
    }
  ],
  "relatedKnowledgeIds": [],
  "sources": [],
  "status": "draft",
  "updatedAt": "2026-09-15T00:00:00Z"
}
```

两个示例均标记为草稿。内容核对后再改为 `published`，正式目录及搜索只收录已发布内容；编辑预览可展示草稿。

## 6. 单词格式及完整实例

单词专用字段包括词形、音标、多个释义、例句、词根、助记和搭配。不要把这些字段塞进数学与政治的知识点结构。

`schemaVersion`、`id`、`contentVersion`、`courseId`、`word`、`pronunciations`、`senses`、`examples`、`collocations`、`tags`、`sources`、`status`、`updatedAt` 必填。`root` 与 `mnemonicMarkdown` 可选。`senses` 至少一项；例句至少一项；其他数组可以为空。

- `pronunciations[]`：`accent` 为 `uk` 或 `us`，`ipa` 为音标，`audioUrl` 可选。
- `senses[]`：`partOfSpeech` 为词性，`meaningZh` 为中文释义。
- `examples[]`：`id`、`sentence`、`translationZh` 必填。
- `root`：`display`、`explanationMarkdown` 必填；词源不确定时省略整个对象。
- `collocations[]`：`phrase`、`meaningZh` 必填。
- 通用版本、来源和状态字段沿用第 3、5 节约定。

```json
{
  "schemaVersion": "1.0",
  "id": "word-persist",
  "contentVersion": 1,
  "courseId": "english",
  "word": "persist",
  "pronunciations": [
    { "accent": "uk", "ipa": "pəˈsɪst" }
  ],
  "senses": [
    { "partOfSpeech": "v.", "meaningZh": "坚持；持续" }
  ],
  "examples": [
    {
      "id": "effort",
      "sentence": "If you persist in your efforts, you will make progress.",
      "translationZh": "如果坚持努力，你就会取得进步。"
    }
  ],
  "mnemonicMarkdown": "把自己想成一棵站稳的树：风会来，但你依然坚持。",
  "collocations": [
    { "phrase": "persist in doing sth.", "meaningZh": "坚持做某事" }
  ],
  "tags": ["考研英语", "核心词汇", "动词"],
  "sources": [],
  "status": "draft",
  "updatedAt": "2026-09-15T00:00:00Z"
}
```

未提供音频文件时省略 `audioUrl`，由前端决定是否提供语音朗读。音标数据本身不代表一定存在可播放音频。

## 7. 个人学习数据格式

以下是一名用户的数据导出快照，便于理解关联。实际数据库中可以拆成用户设置、阅读记录、单词记录、计划项与学习事件等表。

示例引用前文内容 ID；仅用于联调。正式使用前需发布对应内容。界面“正在阅读”应在打开详情时写入，完成阅读应通过“标记已读”等明确动作写入，避免一打开页面就计为完成。

```json
{
  "schemaVersion": "1.0",
  "userId": "demo-user-001",
  "timezone": "Asia/Taipei",
  "settings": {
    "examYear": 2027,
    "englishExam": "english-1",
    "mathExam": "math-1"
  },
  "goalHistory": [
    {
      "effectiveFrom": "2026-09-15",
      "dailyWords": 50,
      "dailyKnowledgePoints": 3
    },
    {
      "effectiveFrom": "2026-09-16",
      "dailyWords": 30,
      "dailyKnowledgePoints": 2
    }
  ],
  "readingProgress": [
    {
      "knowledgeId": "kp-calculus-limit-concept",
      "status": "completed",
      "lastSectionId": "removable-gap",
      "startedAt": "2026-09-15T02:20:00Z",
      "lastOpenedAt": "2026-09-15T02:20:00Z",
      "completedAt": "2026-09-15T02:25:00Z",
      "completedContentVersion": 1
    },
    {
      "knowledgeId": "kp-politics-practice-cognition",
      "status": "reading",
      "lastSectionId": "cognition-guidance",
      "startedAt": "2026-09-15T02:30:00Z",
      "lastOpenedAt": "2026-09-15T02:30:00Z",
      "completedAt": null,
      "completedContentVersion": null
    }
  ],
  "wordProgress": [
    {
      "wordId": "word-persist",
      "familiarity": "fuzzy",
      "lastReviewedAt": "2026-09-15T02:10:00Z"
    }
  ],
  "favoriteWordIds": ["word-persist"],
  "planItems": [
    {
      "id": "plan-demo-001",
      "knowledgeId": "kp-politics-practice-cognition",
      "status": "pending",
      "sortOrder": 10,
      "addedAt": "2026-09-15T02:00:00Z",
      "completedAt": null
    }
  ],
  "learningEvents": [
    {
      "id": "event-demo-001",
      "type": "word_reviewed",
      "contentId": "word-persist",
      "contentVersion": 1,
      "occurredAt": "2026-09-15T02:10:00Z",
      "localDate": "2026-09-15",
      "timezone": "Asia/Taipei",
      "familiarity": "fuzzy"
    },
    {
      "id": "event-demo-002",
      "type": "knowledge_completed",
      "contentId": "kp-calculus-limit-concept",
      "contentVersion": 1,
      "occurredAt": "2026-09-15T02:25:00Z",
      "localDate": "2026-09-15",
      "timezone": "Asia/Taipei"
    }
  ],
  "updatedAt": "2026-09-15T02:30:00Z"
}
```

### 个人记录规则

- 阅读状态：`reading`、`completed`；没有记录表示未开始。`lastSectionId` 可为 `null`，否则须对应当前知识点的要点或例子 ID。
- 单词熟悉程度：`unknown` 不认识、`fuzzy` 模糊、`familiar` 熟悉；未学习单词没有记录。
- 计划状态：`pending`、`completed`。每个用户对同一知识点最多一个有效计划项。阅读完成时可同步完成计划；移除计划不删除阅读历史。
- 学习事件：`word_reviewed` 引用单词，并要求 `familiarity`；`knowledge_completed` 引用知识点。内容 ID 类型要与事件类型匹配。
- 事件 ID 在创建时生成并保持不变；同步重试按同一 ID 去重，避免重复计数。
- `englishExam`：`english-1`、`english-2`；`mathExam`：`math-1`、`math-2`、`math-3`。它们是个人目标设置，不能据此自动推断知识点适用范围；如以后需要考纲筛选，应另增经维护的适用范围字段。
- `dailyWords`、`dailyKnowledgePoints` 为正整数；当前设计的选项分别为 `[20,30,50,80]`、`[1,2,3,5]`。
- 目标变更记录保留生效日期。当天选取 `effectiveFrom` 不晚于当天的最近一条记录；新目标从次日生效，同一生效日只保留一条目标配置。
- `completedContentVersion` 保存完成时看到的版本。内容更新后可提示“讲解有更新”，不静默清空已读状态。
- 本地多账号使用不同的存储键，例如 `cishu:user:demo-user-001:v1`。

## 8. 搜索索引与统计口径

### 搜索索引

由已发布知识点生成轻量索引；详情需要时再加载正文。以下示例假设数学知识点已审核发布：

```json
{
  "schemaVersion": "1.0",
  "generatedAt": "2026-09-15T03:00:00Z",
  "items": [
    {
      "id": "kp-calculus-limit-concept",
      "contentVersion": 1,
      "subjectId": "calculus",
      "chapterId": "ch-calculus-limits",
      "title": "函数极限的概念",
      "summary": "理解自变量趋近某一点时，函数值无限接近某个确定值的含义。",
      "tags": ["极限", "函数", "无限接近"],
      "sortOrder": 10,
      "estimatedMinutes": 5,
      "contentPath": "knowledge/calculus/kp-calculus-limit-concept.json",
      "searchText": "高等数学 高数 函数与极限 函数极限的概念 极限 函数 无限接近"
    }
  ]
}
```

`contentPath` 相对于 `data/content/`。`searchText` 由课程、学科及简称、章节、标题、摘要、标签和去掉格式后的讲解文字拼接生成；上例为便于阅读的缩短演示值，生成器应输出完整结果。搜索时对查询与索引统一去首尾空格、转换英文大小写。

### 统计规则

| 展示项 | 建议计算方法 |
| --- | --- |
| 章节总知识点 | 当前章节中 `published` 知识点数量 |
| 章节已学数量 | 当前章节中已完成的已发布知识点去重数量 |
| 章节进度 | 已学数量 ÷ 总知识点；总数为 0 时显示“暂无内容” |
| 今日单词数 | 当日本地日期的 `word_reviewed` 事件，按单词 ID 去重 |
| 今日知识点数 | 当日本地日期的 `knowledge_completed` 事件，按知识点 ID 去重 |
| 今日待办数 | 单词目标与知识点目标中尚未达标的类别数，范围 0～2 |
| 今日进度环 | 单词达成率与知识点达成率的平均值，各自最高计 100% |
| 累计学习量 | 所有历史完成事件，按对应内容 ID 去重；可包含后续归档内容 |
| 每周柱状图 | 按本地日期分组，统计每天去重内容数；没有事件的日期补 0 |
| 连续学习天数 | 从今天向前统计有学习事件的连续日期；今天尚未学习则从昨天开始 |
| 接着上次学 | 从阅读记录取 `lastOpenedAt` 最新的内容，跳转至 `lastSectionId` |

同一内容当天重复阅读或反复复习只计一次当日数量；不同日期再次完成可计入各自当日数量，但累计内容数只计一次。每周总量等于每日数量之和，含跨日复习，不等于一周内不同内容的总数。

知识点总数、已读数、进度百分比、待办数量均由数据计算，不把原型中的示例常量写入内容文件。尤其不能用累计已读数量计算“今日完成”，或用计划项数量判断今日目标是否完成。

## 9. 从当前设计稿迁移

| 当前原型字段或结构 | 建议存放方式 |
| --- | --- |
| `subjects[]` 中名称、简称、章节 | 拆入 `catalog.json` 的学科与章节 |
| `topics[]`、`notes[]`、`concepts[][]` | 合并为独立知识点对象，取消平行数组与位置关联 |
| `concepts[].formula` | 数学转 `anchor` 公式，政治转 `anchor` 概念文字 |
| `concepts[].steps` | 转为带稳定 ID 的 `keyPoints[]` |
| `concepts[].example` | 转为 `examples[]` |
| `subjects[].progress`、`total` | 分别由个人阅读记录与已发布内容计算 |
| `readTopics` | 转为 `readingProgress`；原型没有真实阅读时间时不补造历史事件 |
| `lastTopic` | 用最近打开的阅读记录和小节位置表示 |
| `planned` | 转为 `planItems` |
| `answered[word]` | 转为以稳定单词 ID 关联的 `wordProgress` |
| 数组下标形式的 `favorites` | 转为 `favoriteWordIds` |
| `goals.words`、`goals.knowledge` | 转为带生效日期的 `goalHistory` |

旧 ID 如 `calculus-0` 需要建立明确映射表。政治原型的大专题拆成多个知识点时，旧已读记录不可自动把所有子知识点标为已读；可保留旧导入记录供迁移追溯。

## 10. 录入与导入检查

1. JSON 能成功解析，必填字段存在，枚举及数据类型符合本文约定。
2. 内容 ID 不重复；章节、学科、相关知识点与个人记录的关联 ID 有效。
3. 每个知识点包含 3 个理解要点和至少 1 个讲解例子，小节 ID 不重复。
4. 数学公式通过实际渲染检查；政治概念卡使用文本格式。
5. 草稿不进入正式搜索；内容发布或归档后重新生成索引，更新正文缓存版本。
6. 进度、计划和统计按用户隔离；同步时按事件 ID 去重。
7. 原型迁移不把示例进度或缺失时间当作真实历史；上线后按实际事件累计。

建议先按目录、知识点、单词、个人记录这四类数据落地。后续扩充视频、图片或更多章节时，通过明确增加字段和结构版本演进，保持现有 ID 与个人记录关联稳定。
