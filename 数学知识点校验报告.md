# 数学知识点内容校验报告

> 校验对象：`data/content/knowledge/{calculus,algebra,probability}/*.json`（960 个知识点）
> 源：`data/math/tree.ts` + `lectures.ts` + `enhancements.ts` → `tools/build-content.ts`
> 工具：`python tools/_math_audit.py [--dump-anchors]`　时间：2026-09-18

## 零、结论

**结构完整，但内容不能用。** 三个问题按严重程度排：

| 级别 | 问题 | 规模 |
|---|---|---|
| **P0 合规** | 内容转写自**商业教辅课程**（张宇《基础30讲》），字段里带出处、正文里点名、还引用例题编号 | **93 / 960 个知识点** |
| **P0 正确性** | 存在明确的数学错误与误导（如二元极限用一元左右极限判据） | 已定位 4 类，见第二节 |
| **P1 合理性** | 内容与知识点标题普遍不符 + 87.7% 模板复用 | 842 / 960 条为逐字重复 |

根因不是「写错了」，而是**生成器的分派逻辑有缺陷**（第五节，已定位到具体行号）。

---

## 一、P0 合规：内容转写自商业课程，且带着出处

- **93 个知识点的 `sources` 写着课程出处**，例如：
  - `张宇基础30讲·线代第1讲`（42 条）、`·高数第1讲`（24 条）、`·概率第1讲`（9 条）、`·高数第5讲`（6 条）…
- **正文里直接点名并引用例题编号**，例如：
  - 「张宇强调的完整清单（x→0）：…」
  - 「张宇特别提醒：遇到因式超过三个的乘积不要硬展开求导…」
  - 「令 F(x)=f(x)−x，端点值 F(½)·F(1)<0，由零点定理得证（**张宇例6.1**）」
  - 「f′(1)=u′(1)v(1)=−π·99!/2（**张宇例4.1**）」
  - 还有整段「张宇从几何本质讲行列式…」
- 源码里也留着这条设计意图（`lectures.ts` L46）：
  「内容出处（对应张宇基础30讲的讲次），有出处时页面会在标签区展示」。
- 目前 `pages-knowledge/topic/topic.vue` **没有**渲染 `sources`（好事），但数据、正文表述和出处字段都还在，**上线前必须处理**：要么取得授权，要么重写为原创表述（去掉人名、例题编号与逐字转写）。

> 这不是我「感觉像」——93 条的 `sources` 字段与正文里的「张宇例X.Y」都是可直接 grep 的证据。

---

## 二、P0 正确性：明确的数学错误 / 误导

1. **「二重极限」（数一、数二各一条）**：整条在讲**一元**函数的左右极限。
   - anchor：`limₓ→a f(x)=L ⇔ 左极限=右极限=L`；要点：「左、右极限必须分别看」。
   - **错**：二元极限没有「左右极限」这回事，必须**沿任意路径**趋近且极限一致。按现有内容做题会得出错误结论。
   - 生成器里其实**有**写对的模板（L975「所有路径都要一致」，例子用 xy/(x²+y²) 沿两条路径得 ±1/2 判不存在），但被 L617 的宽泛规则抢先，永远走不到（见第五节）。
2. **「莱布尼茨公式」（一元微分学 / 高阶导数）**：整条（摘要 + 公式 + 要点 + 例子 `∫₀¹2x dx=[x²]₀¹=1`）是**牛顿—莱布尼茨公式**（定积分）。
   - 应为 `(uv)⁽ⁿ⁾=Σₖ₌₀ⁿ Cₙᵏ u⁽ᵏ⁾v⁽ⁿ⁻ᵏ⁾`。
   - 成因已复现：`fuzzyDirect` 的「key 包含标题」分支把「莱布尼茨公式」匹配到了 `DIRECT` 里的「牛顿—莱布尼茨公式」。
3. **「弧微分」（一元微分学 / 曲率）**：公式与要点都在讲曲率（`κ=|y″|/(1+y′²)^(3/2)`）。
   - 应为 `ds=√(1+y′²) dx`。
4. **跨学科串味：「随机变量的数字特征 / 线性相关意义」（概率）**：整条讲的是**线性代数**的向量组线性相关（`k₁α₁+…+kₘαₘ=0`）。
   - 概率里这条应指「|ρ|=1 ⇔ X 与 Y 以概率 1 线性相关（Y=aX+b）」。

---

## 三、P1 合理性：内容与标题不符

「一条公式被套到多个不相干知识点上」的典型（左=标题，右=实际挂的内容）：

| 章节 | 知识点 | 实际内容 | 判断 |
|---|---|---|---|
| 线代 / 行列式 | 二阶、三阶、n 阶行列式 | `(uv)⁽ⁿ⁾=ΣCₙᵏu⁽ᵏ⁾v⁽ⁿ⁻ᵏ⁾` | 高阶导数的莱布尼茨公式 |
| 线代 / 向量 | 正交化、单位化 | `P(A∪B)=P(A)+P(B)−P(AB)` | 概率加法公式 |
| 线代 / 线性方程组 | 非齐次通解 = 特解 + 齐次通解 | `y′=f(x)g(y) ⇒ dy/g(y)=f(x)dx` | 可分离变量微分方程 |
| 线代 / 特征值 | 特征方程 `|λE−A|=0` | `y″+py′+qy=0 ⇒ r²+pr+q=0` | 微分方程的特征方程（同名混淆） |
| 线代 / 二次型 | 可逆线性变换不改变正负惯性指数 | `A⁻¹=adj(A)/|A|` | 伴随矩阵求逆 |
| 线代 / 矩阵 | 28 个点（零矩阵/初等变换/分块/求秩…） | `(AB)ᵀ=BᵀAᵀ；IA=AI=A` | 只有「转置」一条真正对应 |
| 概率 / 数字特征 | 连续型期望、0-1、二项、泊松、均匀、指数、正态（8 点） | `P(a<X<b)=∫f；Z=(X−μ)/σ` | 这 8 点要的是各分布的 E/D |
| 概率 / 大数定律与 CLT | 独立同分布 CLT、棣莫弗—拉普拉斯等（4 点） | `lim f(x)=L ⇔ 左右极限=L` | 一元极限判据，完全无关 |
| 概率 / 参数估计 | 令导数为零并检查参数范围（MLE） | `P(A∪B)=P(A)+P(B)−P(AB)` | 加法公式 |
| 高数 / 常微分方程 | 变化率模型 | `|a|=√(a₁²+…+aₙ²)` | 向量模长 |
| 高数 / 无穷级数 | 莱布尼茨判别法（交错级数） | `lim aₙ/bₙ=c∈(0,∞) ⇒ 同敛散` | 比较判别法极限形式 |
| 高数 / 曲线积分 | 曲线积分转二重积分（格林公式） | `∬ᴰf dA=∫∫f dydx` | 二重积分直角坐标计算 |
| 高数 / 一元微分学 | 复合函数求导 | `(f∘g)(x)=f(g(x))` | 复合函数的**定义**，不是求导法则 |
| 高数 / 多元微分学 | 方向导数定义 | `f′(x₀)=lim[f(x₀+h)−f(x₀)]/h` | 一元导数定义 |
| 高数 / 多元微分学 | 偏导数几何意义 | `y−f(x₀)=f′(x₀)(x−x₀)` | 一元切线（二元应为 `z−z₀=fₓ(x−x₀)+fᵧ(y−y₀)`） |
| 高数 / 向量代数 | 平行四边形/三角形面积 | `总量=∫微元量` | 面积应为 `|a×b|` |

---

## 四、量化证据

| 指标 | 数值 |
|---|---|
| 知识点总数 | 960（高数 529 / 线代 283 / 概率 148） |
| **唯一内容组数** | **118** |
| **模板重复率** | **87.7%**（842 条与他人逐字相同） |
| `examples` 标题 | 960/960 全是「讲解例子」 |
| `anchor.caption == summary` | 465（48.4%） |
| 要点正文 == 标题（等于没写） | **2227 / 2880（77.3%）** |
| 带商业课程出处的 `sources` | 93（见第一节） |

章节内公式复用（越集中越说明内容与标题不符）：

| 章节 | 知识点 | 只用了几条公式 | 一条最多被几个点共用 |
|---|---|---|---|
| 线代 / 矩阵（数一） | 41 | 4 | **28** |
| 线代 / 矩阵（数二） | 35 | 4 | 26 |
| 高数 / 常微分方程（数一） | 32 | 4 | 22 |
| 线代 / 二次型（数一） | 23 | 4 | 20 |
| 概率 / 一维随机变量 | 22 | 3 | 20 |
| 线代 / 行列式（数一） | 19 | **2** | 18 |

---

## 五、根因：生成器的三个机制（已定位到行号）

**1. 正文规则只有 77 条，要覆盖 960 个知识点** → 必然大面积复用（87.7% 重复率的来源）。

**2. `scopedBody()` 的分派键是 `section + title`，不含学科/章节；规则按源码顺序匹配，宽泛规则写在前面**（`lectures.ts` L545 `const scope = section + ' ' + title`）。

已核实的例子——「二重极限」为什么拿到一元极限的正文：

```
L617: if (/左极限|右极限|函数极限|无穷远处极限|x→x0|x→∞|极限/.test(scope)
         && !/洛必达|第一重要|第二重要|工具箱|等价无穷小|积分|级数/.test(scope)) {
L620:    '极限研究的是变量靠近某点时函数值的趋势…'      ← 二重极限实际拿到的就是这条
...
L975: if (/二重极限|多路径|极限不存在|极坐标处理二元极限/.test(scope)) {   ← 专门写对的规则，永远走不到
```

L617 的排除名单里没有「二重极限 / 多元」，于是它先命中；L975 的专用规则被**遮蔽**。
同一机制导致「概率章节的『线性相关意义』拿到线代正文」（标题含「线性相关」，命中 L533 的学科分派）。

**3. `fuzzyDirect()` 的模糊匹配只看字符串包含关系，不看学科**（L1679-1690）：

```ts
for (const key of keysDesc) if (key.length >= 4 && key !== title && title.includes(key)) return DIRECT[key]
for (const key of keysAsc)  if (key !== title && key.includes(title))               return DIRECT[key]
```

已复现：「莱布尼茨公式」被第二轮的 `key.includes(title)` 命中到 `牛顿—莱布尼茨公式` → 整条内容张冠李戴。

---

## 六、做对的部分（别一起推翻）

1. **章节划分与考研大纲一致**：数一 9 章高数（含空间解析几何、多重积分、曲线曲面积分、无穷级数），数二 6 章（无级数/空间几何/曲面积分/三重积分），线代 6 章两卷共用，**概率只在数一** —— 都对。
2. **公式池本身基本是标准结论**：117 条唯一公式逐条看过，绝大多数写对了（等价无穷小、中值定理、曲率、克莱姆法则、切比雪夫不等式、CLT、格林/高斯/斯托克斯、傅里叶系数…）。
3. 结构层面（id 规范、字段完整、`status: published`）没问题；**问题在「内容挂错地方」和「来源合规」**。

---

## 七、修复建议（按优先级）

1. **合规（P0，先做）**：清掉转写内容——93 条 `sources` 里的课程出处、正文里的人名与「张宇例X.Y」编号，以及逐字转写的段落。要么拿到授权，要么改成原创表述。
2. **正确性（P0）**：修第二节 4 类错误。二重极限不必重写——**把 L975 的专用规则移到 L617 之前**（或给 L617 的排除名单加上「二重极限|多元|二重」）即可用回已经写对的那条；莱布尼茨公式改掉 `fuzzyDirect` 的误命中。
3. **分派架构（P1，治本）**：把学科/章节传进 `scopedBody`，先按学科限定规则池，再在池内按「具体规则优先于宽泛规则」排序（现在 77 条平铺、顺序即优先级，极易遮蔽）。
4. **体例（P1）**：要点正文不要复读标题（77%）；例子标题要具体（现在 960 条全叫「讲解例子」）；caption 不要直接复制 summary。
5. **加校验防回归**（照政治题库 `_test-politics-rules.mjs` 的做法）：
   - 同一章节内一条 anchor 最多被 K 个知识点共用（K=1~2）；
   - 要点正文 ≠ 标题；
   - `sources` 不得出现具体机构/课程名；
   - 关键词与学科一致性（概率章节不得出现 αᵢ、Σkᵢαᵢ 这类线代符号）。
6. **短期兜底**：如果暂时没人手重写，建议**先隐藏 anchor 与要点正文**（只留标题 + 章节结构），把数学模块做成「知识点目录」，比展示错公式更安全。

---

## 八、怎么复现

```bash
python tools/_math_audit.py                 # 完整性 / 重复度 / 章节复用 / 机械异常
python tools/_math_audit.py --dump-anchors  # 打印去重后的 117 条公式，逐条核对用
```

写审计脚本时踩过的三个误报（都已在脚本里修掉，别再把它们当缺陷）：

- 「略」单独一个字会命中「策略 / 忽略 / 省略」→ 占位词判据必须带上下文；
- `|` 是绝对值符号（`|aₙ−A|<ε`），不是 markdown 表格残留；
- 公式里出现中文标点（`f(x+T)=f(x)，T>0`）是设计稿的既定写法。

---

## 九、本轮修复进展（2026-09-18 晚）

用户提供了《基础30讲》三册扫描件（高数 586 页 / 线代 213 页 / 概率 173 页，共 972 页），
要求据此修复错误并补全内容。**先把「生成器分派错误」和「合规」两类问题清掉**（这两类改动小、影响面大）：

### 已修复（均已重建 + 回归检查通过）

| # | 问题 | 修法 | 效果 |
|---|---|---|---|
| 1 | 「二重极限」用一元左右极限判据（**数学错误**） | `lectures.ts` L617 的排除名单补上「二重/多元/偏导/梯度/方向导数」，L985 补上「极限/连续/偏导/可微…」 | 现在拿到写对的「沿任意路径」正文（`lim₍ₓ,ᵧ₎→(0,0)f(x,y) 必须与路径无关`） |
| 2 | 「莱布尼茨公式」= 牛顿—莱布尼茨公式 | `fuzzyDirect` 收紧：删掉「标题含条目名」方向，只保留「条目名 = 标题 + ≤2 字」 | 现在拿到 `(uv)⁽ⁿ⁾=Σ Cₙᵏu⁽ᵏ⁾v⁽ⁿ⁻ᵏ⁾` |
| 3 | 「弧微分」= 曲率公式 | 给弧微分单独建规则（`ds=√(1+y′²)dx`），曲率规则不再吃它 | ✅ |
| 4 | 概率「线性相关意义」= 线代向量组 | 线代「线性相关」规则加概率侧排除（随机变量/期望/方差/协方差/相关系数） | 现在走概率的相关系数正文 |
| 5 | 「二阶、三阶、n 阶行列式」= 高阶导数 | L552 的 `二阶、三阶` 补成 `二阶、三阶导数` | 现在走行列式规则 |
| 6 | 「复合函数求导」「分段函数求导」「方向导数定义」「偏导数几何意义」被模糊匹配抢走 | 同 #2（收紧后各自走专用规则 L682 / L734 / L1027 / L988） | 锚点分别变成链式法则、左右导数、`∇f·u`、`dz=fₓdx+fᵧdy` |
| 7 | **合规**：展示内容里出现「张宇强调…」「（张宇例6.1）」「来源：张宇基础30讲·线代第1讲」 | ① `_clean-math-attribution.py` 清理 15 处人名/例题编号；② `build-content.ts` 不再把 `source` 拼进 `bodyMarkdown` 渲染 | **展示内容含课程名/人名的知识点：94 → 0** |

### 新增的防回归

- `tools/check-math-content.mjs`：10 条「关键映射」断言（上面每条修好的映射都固化了，
  含数一/数二两处二重极限）+ 1 条合规断言（展示字段不得出现课程名/人名），
  并打印重复率作为「内容要重写」的量化目标。**已做双向验证**：把断言值改回旧内容即失败。
- `tools/_math_audit.py`、`tools/_clean-math-attribution.py`、`tools/_pdf_render.py`（扫描件渲染工具）。

### 量化变化

| 指标 | 修前 | 修后 |
|---|---|---|
| 唯一摘要 | 118 / 960 | **128 / 960** |
| 模板重复率 | 87.7% | 86.7% |
| 展示内容含课程名/人名 | 94 条 | **0 条** |
| 关键映射正确性 | 4 类明确错误 | 全部通过断言 |

> 重复率只降了 1 个百分点——**说明「补全内容」才是主要工作量，不可能靠改规则解决**。

## 十、剩余工作：内容重写（这才是大头）

按「摘要重复 或 锚点被 ≥6 个知识点共用」统计，**944 / 960 个知识点都算薄弱**，
所以这不是修几条的问题，而是**逐章重写**。工作量最大的章节：

| 模块 | 章节 | 薄弱 / 总数 |
|---|---|---|
| 数学二 | 一元函数微分学 | 55 / 55 |
| 数学二 | 函数、极限与连续 | 47 / 47 |
| 数学一 | 一元函数积分学 | 47 / 48 |
| 数学一 | 矩阵 | 41 / 41 |
| 数学一 | 多元函数微分学 | 36 / 38 |
| 数学一 | 无穷级数 | 33 / 33 |

**建议的推进方式（每批可交付、可验收）**：

1. 以「讲」为单位推进（书里的讲次 ≈ 我们的章节）：先渲染该讲的 PDF 页 → 逐页核对定义/定理/例题 →
   为**该章节的每个知识点**写 `summary / anchor / 要点 / 例子`（原创表述，不抄书）；
2. 每批结束后跑 `build:content` + `validate:content` + `check-math-content.mjs`，
   并把这批的映射加进 GOLDEN 断言；
3. 优先顺序建议：**先高数（考的人最多、错误影响最大）→ 再线代 → 最后概率**；
   每科内部按「薄弱率 × 考频」排。

**关于扫描件**：三册都是纯图片（每页 0 文本），本机没有 OCR，
所以走「`tools/_pdf_render.py` 渲染成图 → 逐页阅读」这条路；
972 页不可能一次读完，必须按讲分批。

### 精编内容 第 1 批：概率 · 第 1 讲「随机事件与概率」（30 / 30）

新增 `data/math/curated.ts`——**按知识点标题精确索引**的精编内容层，在 `getMathLecture` 里
**优先级高于规则生成**（命中即不再走 77 条规则）。这样「一个知识点一份内容」，
写一个覆盖一个，且不会反过来影响其它知识点。

- 覆盖：概率册第 1 讲全部 30 个知识点（随机试验 → 独立与互斥的区别）；
- 写法：`explanation` 首句自成结论（页面上的 summary 就是它的截断）、要点写成判断句、
  例子带具体数字、易错点写真实踩坑；**不含任何课程名/人名/例题编号**；
- 效果：唯一摘要 128 → **155**，模板重复率 86.7% → **83.9%**，
  「要点正文 == 标题」2240 → 2196（精编的要点比标题长，不再复读）；
- 回归：`check-math-content.mjs` 新增 6 条精编层断言（德摩根律的 ᶜ、独立与互斥、
  全概率的 Σ、贝叶斯的先验、对立的 1−P(A)、几何概型的测度）——**这 6 条在精编之前都是不通过的**，
  等于把「这 30 个点确实换成了新内容」固化成了断言。

### 精编内容 第 2、3 批：概率第 2 讲（22 点）+ 第 5 讲（9 点）

**累计 61 / 960 个知识点已有精编内容**（第 1 讲 30 + 第 2 讲 22 + 第 5 讲 9）。

- **第 2 讲「一维随机变量及其分布」**（22 点）：定义、分布函数四条性质、分布律、0-1/二项/泊松、
  密度与分布函数的关系、区间概率、均匀/指数/正态/标准正态/标准化、分布函数法与单调变换公式；
- **第 5 讲「大数定律与中心极限定理」**（9 点）：切比雪夫不等式、三个大数定律、频率稳定性、
  独立同分布 CLT、棣莫弗—拉普拉斯、CLT 的标准化、二项分布正态近似。

**本批修掉的两个新问题**：

1. **重名标题会串章节**（架构级隐患）：`定义` 在全库出现 **22 次**（矩阵/向量/特征值/二次型/多重积分/
   二重积分/数字变量/数理统计…），`极限性质` 4 次，`标准化` 2 次。精编表原来只按标题索引，
   概率的「定义」会被套到矩阵、向量上。修法：键支持 `章节标题/知识点标题` 形式
   （`getMathLecture(topic, section, chapter)` 把章节传下去），并在 `check-math-content.mjs` 里
   断言「不带 / 的键在全库唯一」——重名就会失败，逼着写的人限定章节。
2. **概率第 5 讲整章被一元极限规则吞掉**：`大数定律与中心极限定理` 这一章的 9 个点，
   scope 里都带「极限」或「正态」，于是被 L617 的一元极限规则抢走（拿到「左右极限判据」）。
   在排除名单里补上 `中心极限|大数定律|棣莫弗|标准化|正态|切比雪夫` 后，落到概率规则上；
   随后该章 9 个点全部精编，彻底解决。

### 量化进展

| 指标 | 起点 | 第 1 批后 | 第 2、3 批后 |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 30 | **61 / 960** |
| 唯一摘要 | 118 | 155 | **185** |
| 模板重复率 | 87.7% | 83.9% | **80.7%** |
| 要点正文 == 标题 | 77.3% | 76.3% | **74.4%** |

> 规律很清楚：**每精编 1 个知识点，唯一摘要 +1**。要把重复率降到 0，需要把 960 个点全部精编，
> 按每批 20~30 个点的节奏，大约还需 30 批。

### 精编内容 第 4~7 批：概率整册完成（148 / 148）

| 章节 | 知识点 | 状态 |
|---|---|---|
| 随机事件与概率（第 1 讲） | 30 | ✅ 第 1 批 |
| 一维随机变量及其分布（第 2 讲） | 22 | ✅ 第 2 批 |
| 大数定律与中心极限定理（第 5 讲） | 9 | ✅ 第 3 批 |
| 多维随机变量及其分布（第 3 讲） | 13 | ✅ 第 4 批 |
| 随机变量的数字特征（第 4 讲） | 21 | ✅ 第 5 批 |
| 数理统计基本概念（第 6 讲） | 21 | ✅ 第 6 批 |
| 参数估计（第 6 讲） | 17 | ✅ 第 7 批 |
| 假设检验（第 6 讲） | 15 | ✅ 第 7 批 |
| **合计** | **148** | **概率册 100% 精编** |

### 架构再次加固（三段键 + 两条防呆断言）

写这批时发现同一章内还有重名：`随机变量的数字特征` 里**方差、协方差、相关系数三节各有一个「定义」**。
只到章节级仍然区分不了，于是查表升级为三级：

```
CURATED['章节/小节/标题'] || CURATED['章节/标题'] || CURATED['标题']
```

`build-content.ts` 改为把「去掉编号后的小节标题」传给 `getMathLecture`。

同时补了两条防呆断言（都踩过坑才加的）：

1. **每个精编键都要能命中知识点**——键写错会**静默不生效**（内容写了却用不上，最难发现）；
2. **只按标题索引的键必须全库唯一**——重名必须写成「章节/标题」。
   ⚠️ 写这条时正则用了 `[^:]`，而它**会跨行匹配**，把下一条目一起吞进来，导致断言**空转通过**；
   必须写成 `[^:\n]`。另外标题本身可能含「/」（`P(A|B)=P(AB)/P(B)`、`离散型/连续型`），
   所以键的合法性判断要「三种解释任一命中即可」。

### 量化进展（概率整册完成）

| 指标 | 起点 | 概率第 1~3 批后 | **概率整册完成后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 61 | **148 / 960（概率 100%）** |
| 唯一摘要 | 118 | 185 | **266** |
| 模板重复率 | 87.7% | 80.7% | **72.3%** |
| 要点正文 == 标题 | 77.3% | 74.4% | **70.6%** |

### 下一步

概率册已收口，剩下 **812 个知识点**（高数 529 + 线代 283）。
建议顺序：**高数 → 线代**（高数考的人最多、之前发现的错误也最集中）。
高数册 586 页扫描件，页码偏移需要先渲染一页核对（概率册是「书内页 = PDF 页 − 9」）。

### 精编内容 第 8 批：高数第 1 章「函数、极限与连续」（数一 29 + 数二 47 = 76 点）

- 内容覆盖：函数三要素与四条性质、复合/反函数/分段/隐函数、数列极限（ε-N、收敛性质、存在准则）、
  函数极限（x→x₀、左右极限、x→∞、极限性质）、无穷小与无穷大、等价无穷小、两个重要极限、
  七种未定式与 8 类求极限方法（直接代入 / 因式分解 / 有理化 / 通分 / 提最高次 / 等价替换 /
  夹逼 / 洛必达 / 泰勒 / 换元）、连续性与间断点分类、闭区间连续函数四条性质。
- 高数册页码偏移已核实：**书内页 = PDF 页 − 6**（PDF p9 = 第 1 讲第 3 页）。

**本批抓到的问题（都由断言发现）**：5 个标题跨章节重名，必须按章节限定——
`单调性`、`复合函数`（也在「一元函数微分学」章）、`奇偶性`（也在「二重积分」章）、
`0/0`、`∞/∞`（也在「一元函数微分学」章）。

**顺带修了一个环境坑**：`build:content` 偶发 `UNKNOWN: unknown error, open <大文件>`，
**失败的那一次会让 `data/generated/app/` 少生成一个文件**（这次丢了 `politics-index.ts`），
于是 `check-bundles` 连环报 ENOENT、`_route-harness` 也挂——看着像分包配置坏了。
已在 `check-bundles.mjs` 开头加「生成物齐全」守卫，把问题说清楚。

### 量化进展

| 指标 | 起点 | 概率册完成后 | **高数第 1 章完成后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 148 | **224 / 960** |
| 唯一摘要 | 118 | 266 | **300** |
| 模板重复率 | 87.7% | 72.3% | **68.8%** |
| 要点正文 == 标题 | 77.3% | 70.6% | **68.8%** |

### 剩余工作量

高数还剩 13 章（453 点）：一元函数微分学（40+55）、一元函数积分学（48+43）、
向量代数与空间解析几何（36）、多元函数微分学（38）、多重积分（23）、二重积分（20）、
曲线积分与曲面积分（25）、无穷级数（33）、常微分方程（32+32）、多元函数微积分学（28）……
另有线代 12 章（283 点）。按每批一章、每章 20~50 点的节奏推进。

### 精编内容 第 9 批：高数第 2 章「一元函数微分学」（数一 40 + 数二 55 = 95 点）

全册最大的一章。内容覆盖：导数定义与三种意义、可导与连续、微分与线性主部、一阶微分形式不变性、
基本导数公式、四则/复合/反函数/隐函数/参数方程/对数/分段函数求导、高阶导数与莱布尼茨公式、
四个微分中值定理、泰勒定理（皮亚诺余项与拉格朗日余项、五个常用展开）、洛必达法则
（0/0 与 ∞/∞ 型、条件、多次使用、失效情形）、导数应用（单调性/极值/最值/凹凸/拐点/渐近线/作图）、
弧微分与曲率（定义/公式/半径/曲率圆）。

**这批把之前发现的 3 处错配彻底换成了正确内容**（之前只是靠改规则让它走对分支）：

| 知识点 | 修复后 |
|---|---|
| 复合函数求导 | `dy/dx=dy/du·du/dx`（链式法则，不是复合函数的定义） |
| 莱布尼茨公式 | `(uv)⁽ⁿ⁾=Σ Cₙᵏu⁽ᵏ⁾v⁽ⁿ⁻ᵏ⁾`（不是牛顿—莱布尼茨公式） |
| 弧微分 | `ds=√(1+y′²)dx`（不是曲率公式） |

**断言又抓到两类问题**：
1. 7 个标题跨章节重名需要限定：`几何意义`（特征值/二重积分章也有）、`使用条件`（线性方程组章也有）、
   `e^x`/`sin x`/`cos x`/`ln(1+x)`/`(1+x)^α`（**无穷级数章也有**——同样是泰勒展开点）；
2. 我的键解析器不支持**双引号键**，而 `"dy=f'(x)dx"` 这种标题里含单引号只能用双引号包裹 →
   解析失败导致「键不命中」误报。已修（三种写法都支持）。

### 量化进展

| 指标 | 起点 | 高数第 1 章后 | **高数第 2 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 224 | **278 / 960** |
| 唯一摘要 | 118 | 300 | **345** |
| 模板重复率 | 87.7% | 68.8% | **64.1%** |
| 要点正文 == 标题 | 77.3% | 68.8% | **66.1%** |

### 剩余工作量（按章推进）

用户要求把高数 13 章 + 线代 12 章（共 736 点）都做完。已完成高数前 2 章（171 点）。
剩余高数 11 章约 **358 点**：一元函数积分学（48+43）、常微分方程（32+32）、多元函数微分学（38）、
向量代数与空间解析几何（36）、无穷级数（33）、多元函数微积分学（28）、曲线积分与曲面积分（25）、
多重积分（23）、二重积分（20）；线代 12 章 **283 点**（矩阵 41+35、向量 25+21、行列式 19+18、
线性方程组 20+18、特征值 24+20、二次型 23+19）。
按每批一章、每章 20~50 点推进，约需 12~15 批。

### 精编内容 第 10 批：高数第 3 章「一元函数积分学」（数一 48 + 数二 43 = 91 点）

内容覆盖：原函数与不定积分（定义/原函数族/基本性质）、基本积分公式（幂/指/三角/反三角型）、
换元积分法（第一类凑微分、第二类变量代换、三角代换、根式代换）、分部积分法
（∫u dv=uv−∫v du 与「多项式×指数/三角/对数」的取 u 原则）、特殊积分
（有理式、三角有理式、反三角结构）、定积分（定义/性质/中值定理/积分上限函数/牛顿—莱布尼茨）、
反常积分（无穷限/瑕积分/敛散性）、定积分应用（面积/旋转体/平行截面/弧长/功/压力/引力/平均值）。

**这轮抓到的三类键问题（断言发现）**：
1. **含 `×` 的键必须加引号**：`多项式×指数`、`多项式×三角`、`多项式×对数` 三个标题裸写是语法错误，
   要写成单引号键 `'多项式×指数'`；
2. **同章内也有重名**：数二「一元函数积分学」里**原函数与不定积分、定积分两节各有一个「基本性质」**，
   必须升级为三段键 `一元函数积分学/原函数与不定积分/基本性质` 与 `一元函数积分学/定积分/基本性质`；
3. **跨章节重名 4 个**：`指数函数`（常微分方程章也有）、`定义与几何意义`、`质心`（多重积分章也有）、
   `基本性质`（多重积分、二重积分章也有），全部限定为「一元函数积分学/…」。

**顺带补了一个第 1 章的漏网点**：数二的「基本初等函数」与数一的「基本初等函数与初等函数」是两个不同标题，
之前只写了后者的键，前者一直静默走模板。已补 `函数、极限与连续/基本初等函数` 键，并在覆盖率脚本里
加上「已完成章节必须 100% 有精编键」的人工核对（每批收口时跑）。

### 量化进展

| 指标 | 起点 | 高数第 2 章后 | **高数第 3 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 278 | **349 / 960** |
| 唯一摘要 | 118 | 345 | **405** |
| 模板重复率 | 87.7% | 64.1% | **57.8%** |
| 要点正文 == 标题 | 77.3% | 66.1% | **66.2%** |

校验全绿：check-math-content 41 条、语法/模板/样式/分包/单词模块/路由、validate:content。

### 剩余工作量（每批一章）

高数剩 10 章约 **267 点**：常微分方程（32+32）、多元函数微分学（38）、向量代数与空间解析几何（36）、
无穷级数（33）、多元函数微积分学（28）、曲线积分与曲面积分（25）、多重积分（23）、二重积分（20）；
线代 12 章 **283 点**。按既定顺序「常微分方程 → 多元函数微分学 → …」推进。

### 精编内容 第 11 批：高数第 4 章「常微分方程」（数一 32 + 数二 32 = 64 点）

内容覆盖：基本概念（方程/阶/解/通解/特解/初始条件/初值问题）、一阶方程（可分离变量、
齐次型 y′=φ(y/x) 与 u=y/x 换元、一阶线性与通解公式、伯努利）、可降阶三类
（y″=f(x) 积分两次、缺 y 令 p=y′、缺 x 用 y″=p·dp/dy）、线性方程解的结构
（叠加原理、齐次解张成、非齐次 = 齐次通解 + 特解）、二阶常系数齐次（特征方程与三种根型：
不同实根 / 二重根补 xe^{rx} / 共轭复根出振荡，及高阶推广）、二阶常系数非齐次（多项式、
e^{λx}Pₘ(x) 按 λ 的根重数补 x^k、三角右端 cos/sin 必须两项一起设、和拆项积整体设）、
应用（变化率/运动/增长衰减/几何条件/方向场/弹簧阻尼三种阻尼形态）。

**这轮的键设计**：47 个键覆盖两册 64 点（同名共用）。全库查重后只有两个撞名必须限定：
`常微分方程/通解`（线代「线性方程组」章也有通解）、`常微分方程/指数函数`
（「一元函数积分学」章的指数函数是积分公式点，内容完全不同）。其余 45 个标题全库唯一。

### 量化进展

| 指标 | 起点 | 高数第 3 章后 | **高数第 4 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 349 | **396 / 960** |
| 唯一摘要 | 118 | 405 | **451** |
| 模板重复率 | 87.7% | 57.8% | **53.0%** |
| 要点正文 == 标题 | 77.3% | 66.2% | **65.9%** |

校验全绿：check-math-content 41 条、语法/模板/样式/分包/单词/路由、validate:content。

### 剩余工作量

高数剩 5 个文件主题约 **203 点**：多元函数微分学（数一 38 + 数二部分）、向量代数与空间解析几何
（36）、无穷级数（33）、多元函数微积分学（数二 28）、曲线积分与曲面积分（25）、多重积分
（23）、二重积分（数二 20）。随后按用户要求接着修**线代 12 章 283 点**。

### 精编内容 第 12 批：高数第 5 章「多元函数微分学」（数一 38 点）

内容覆盖：多元函数概念（二元函数/定义域/几何意义/有界闭区域性质）、二重极限与连续
（路径无关、多路径试探、路径法证不存在、极坐标法、连续性）、偏导数（一阶/高阶/混合偏导
可交换条件/几何意义）、全微分（可微定义、dz 公式、偏导-连续-可微关系链）、多元复合
（链式法则、全导数、树状图）、隐函数（一个方程、方程组+雅可比、存在条件）、方向导数
与梯度（定义、单位化、梯度性质、与等值线法向、最大方向导数）、几何应用（切线法平面、
切平面法线）、极值（无条件、必要条件、AC−B² 充分条件、最值、拉格朗日乘数法与多约束）。

**键设计**：38 个标题中 8 个与数二「多元函数微积分学」章重名（二重极限、一阶偏导、混合偏导、
必要条件、二阶充分条件、拉格朗日乘数法、二元函数几何意义、用路径证明极限不存在），
全部限定为「多元函数微分学/…」；数二收口时再写该章版本。GOLDEN 断言同步加固：
二重极限必须含「路径无关」、方向导数定义必须含「∇f」（已含）。

### 量化进展

| 指标 | 起点 | 高数第 4 章后 | **高数第 5 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 396 | **434 / 960** |
| 唯一摘要 | 118 | 451 | **485** |
| 模板重复率 | 87.7% | 53.0% | **49.5%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 13 批：高数第 6 章「向量代数与空间解析几何」（数一 36 点）

内容覆盖：向量运算（坐标/模/单位向量/方向余弦/加减数乘）、数量积（定义/坐标公式/夹角/
垂直判定/投影）、向量积（定义/右手法则/行列式计算/平行判定/面积）、平面方程（点法式/
一般式/截距式/法向量）、空间直线（对称式/参数式/两平面交线/方向向量）、位置关系
（线线-线面-面面平行垂直异面、三种夹角公式、点到平面距离）、曲面（球面/柱面/旋转曲面/
椭球面/双曲面/抛物面/参数方程/投影曲线）。

**键设计**：「定义」章内两节各一个且全库 11 处重名、「夹角」章内两节（数量积角 vs 位置关系角，
内容完全不同）——这两处升级为**三段键**；「单位向量」「投影」与线代「向量」章重名，
限定「向量代数与空间解析几何/…」；两个标题自带「/」加引号。

### 量化进展

| 指标 | 起点 | 高数第 5 章后 | **高数第 6 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 434 | **470 / 960** |
| 唯一摘要 | 118 | 485 | **521** |
| 模板重复率 | 87.7% | 49.5% | **46.4%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 14 批：高数第 7 章「无穷级数」（数一 33 点）

内容覆盖：常数项级数（部分和/敛散定义/必要条件/基本性质）、正项级数（几何级数、p 级数、
比较及极限形式、比值、根值）、交错级数（莱布尼茨、误差 ≤uₙ₊₁）、任意项级数（绝对收敛/
条件收敛/绝对蕴含收敛）、幂级数（收敛半径 R=1/ρ、收敛区间 vs 收敛域、端点单独判、和函数、
逐项求导积分）、六个展开式（1/(1−x)、eˣ、sin x、cos x、ln(1+x)、(1+x)^α）、傅里叶级数
（系数公式、奇偶展开、狄利克雷定理、两个可视化点）。

**键设计**：五个展开式标题（e^x/sin x/cos x/ln(1+x)/(1+x)^α）与微分学章泰勒点重名，
限定「无穷级数/…」；「收敛、发散」标题含顿号——裸标识符里顿号非法，**必须加引号**
（新踩的坑：键的语法检查 TS 报 Invalid character 才发现）。

### 量化进展

| 指标 | 起点 | 高数第 6 章后 | **高数第 7 章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 470 | **503 / 960** |
| 唯一摘要 | 118 | 515 | **544** |
| 模板重复率 | 87.7% | 46.4% | **43.3%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 15 批：数二「多元函数微积分学」（数二 28 点）

内容覆盖：多元函数（概念/定义域/几何意义/有界闭区域性质）、二重极限与连续（路径无关/
多路径/路径法证不存在/极坐标/点连续/区域连续）、偏导数（一阶/二阶/混合）、全微分
（可微定义、dz 公式、关系链）、复合求导（链式法则、**复合函数二阶偏导**、依赖关系图）、
隐函数（一个方程/存在定理/方程组）、极值（驻点/必要条件/AC−B²/极大极小/拉格朗日/应用）。

**键设计**：8 个与数一「多元函数微分学」重名的标题限定「多元函数微积分学/…」；
「二阶偏导」**在本章出现两次**（偏导数节 vs 复合求导节，内容完全不同——概念 vs 对
f_u、f_v 再做链式法则），用三段键 `多元函数微积分学/偏导数/二阶偏导` 与
`多元函数微积分学/多元复合函数求导/二阶偏导` 区分——同章内重名再次证明三段键的必要性。

### 量化进展

| 指标 | 起点 | 高数第 7 章后 | **数二多元微积分后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 503 | **531 / 960** |
| 模板重复率 | 87.7% | 43.3% | **41.7%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 16 批：高数第 9 章「曲线积分与曲面积分」（数一 25 点）

内容覆盖：第一类曲线积分（对弧长/定义/参数化 ds=√(x′²+y′²)dt/柱面面积与质量）、
第二类曲线积分（对坐标/有向曲线/参数化按起终点定限/功）、格林公式（正向边界/
公式本体/补线凑闭/面积公式 A=(1/2)∮x dy−y dx/路径无关/原函数与 u(B)−u(A)）、
第一类曲面积分（对面积 dS=√(1+z_x²+z_y²)dxdy）、第二类曲面积分（对坐标/侧/
法向量余弦定号/投影计算）、高斯公式（外侧/通量/散度三重积分）、斯托克斯公式
（右手法则定边界/旋度）。

**键设计**：「定义」「参数化计算」在第一类/第二类曲线积分两节各一个、含义完全不同
（对弧长 vs 对坐标）——三段键区分；「几何/物理意义」「曲面参数化/投影计算」含「/」加引号。

### 量化进展

| 指标 | 起点 | 数二多元微积分后 | **曲线曲面积分后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 531 | **556 / 960** |
| 唯一摘要 | 118 | 560 | **583** |
| 模板重复率 | 87.7% | 41.7% | **39.3%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 17 批：高数收口——「多重积分」（数一 23 点）+ 数二「二重积分」（20 点）

内容覆盖（数一）：二重积分（定义与几何意义/性质/直角坐标/X 型/Y 型/换序/对称性）、
极坐标二重积分（坐标替换/面积元 r dr dθ/圆扇环区域/坐标选择）、二重积分应用（体积/
曲面面积准备/质量/质心/转动惯量）、三重积分（定义/直角/柱/球坐标/投影法/截面法/对称性）。
数二侧：二重积分概念（定义/几何意义/性质）、直角坐标（X 型/Y 型/先 x 后 y/先 y 后 x/
换序/分区域）、极坐标（两组坐标公式/圆环扇形）、技巧（对称性/奇偶性/区域拆分/坐标系选择）、
应用（体积/薄片质量/质心）。

**键设计（高数最容易踩的一批）**：数一「多重积分」与数二「二重积分」**主题重叠但章节名不同**，
「X 型区域」「Y 型区域」「交换积分次序」「体积」「对称性」等全部要各写一份限定键；
「对称性」在多重积分章内两节各一个（二重积分/三重积分），三段键区分；
「先 x 后 y」「先 y 后 x」键含空格、「x=r cosθ, y=r sinθ」等含标点——都要加引号
（含空格的裸标识符是这轮新踩的语法坑）。

### 🎉 高数册收口

概率册 148 + 高数 10 批 407 点 = **555 点全部精编**，加上数一/数二共册互 share 的部分，
累计精编键 599 / 960。剩线代 12 章 283 点（数一 6 章 + 数二 6 章），按用户要求接下来继续。

| 指标 | 起点 | 曲线曲面积分后 | **高数收口后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 556 | **599 / 960** |
| 唯一摘要 | 118 | 583 | **620** |
| 模板重复率 | 87.7% | 39.3% | **35.4%** |

校验全绿：check-math-content 41 条、全套件、validate:content。
「概率 + 高数全部 555 点 100% 有精编键」已用覆盖率脚本逐点核实。

### 精编内容 第 18 批：线代第 1 章「行列式」（数一 19 + 数二 18 = 37 点）

内容覆盖：行列式概念（二/三/n 阶、排列与逆序数、展开定义思想）、六条性质（转置不变/
换行变号/提公因子/成比例为零/倍加不变/单行线性）、余子式与代数余子式（棋盘符号）、
展开定理（含「异行组合为零」这半条）、计算五法（化三角形/展开/加边/递推/拆分）与
范德蒙德行列式。GOLDEN 断言「二阶、三阶、n 阶行列式必须含面积」（几何意义）已覆盖。

**键设计**：数一、数二标题措辞不同（化三角形 vs 化三角、排列与逆序数 vs 排列与逆序数基本思想、
余子式 M_ij vs M_ij）——**各自写各自的键**；「展开」「加边」「递推」「拆分」两册同名同章共用；
「转置」与矩阵章撞名限定「行列式/转置」。「展开」出现了一次重复键（TS1117）被编译器拦下——
数一数二同名同章的键只写一份。

### 量化进展

| 指标 | 起点 | 高数收口后 | **线代行列式后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 599 | **632 / 960** |
| 唯一摘要 | 118 | 620 | **653** |
| 模板重复率 | 87.7% | 35.4% | **32.0%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 19 批：线代第 2 章「矩阵」（数一 41 + 数二 35 = 76 点，全库最大的一章）

内容覆盖：概念与特殊矩阵（零/单位/数量/对角/三角/对称/反对称/正交）、运算（加减/数乘/
乘法/方阵的幂/转置/运算规律/AB≠BA）、方阵行列式（|AB|=|A||B| 等）、逆矩阵（定义/唯一性/
运算性质/充要条件/伴随求逆）、初等变换与初等矩阵（三类/左行右列/增广求逆）、秩（定义/
最高阶非零子式/阶梯形/最简形/求秩/不等式清单）、等价（概念/PAQ/秩刻画）、分块（加减/乘法/
转置/分块对角/分块求逆）。GOLDEN「矩阵概念必须含 ᵀ」已覆盖。

**键设计（本章 12 个键带限定）**：「对称矩阵」限定矩阵/对称矩阵（与二次型撞名）；
「定义」在三处语义不同——矩阵/转置/定义、矩阵/逆矩阵/定义、矩阵/矩阵的秩/定义三段键；
「运算规律」数一在运算节、数二在转置节，三段键区分；「性质」（数二逆矩阵节）限定。
数一数二措辞不同的标题（方阵的幂 vs 方阵幂、可逆的充分必要条件 vs 可逆充分必要条件、
上/下三角矩阵 vs 三角矩阵、行阶梯形矩阵 vs 阶梯形等）各自写键。上一轮踩过的坑再现：
「转置」第一版写成了裸键（与行列式章撞名），由「键必须唯一命中」断言拦下。

### 量化进展

| 指标 | 起点 | 行列式后 | **矩阵章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 632 | **688 / 960** |
| 唯一摘要 | 118 | 653 | **712** |
| 模板重复率 | 87.7% | 32.0% | **25.8%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 20 批：线代第 3 章「向量」（数一 25 + 数二 21 = 46 点）

内容覆盖：n 维向量（行/列向量、零向量）、线性组合与表示（表达式、增广矩阵判据）、
线性相关性（定义、齐次方程判定、行列式判定、秩判定、常用性质六条、2D/3D 几何可视化）、
极大线性无关组（定义、行变换求法、与原组等价）、向量组的秩（定义、三秩合一）、
向量组等价（互相表示、秩相等的必要性与充要刻画）、内积与正交（内积/长度/正交/单位向量/
标准正交组）、施密特正交化（正交化减投影、单位化、投影向量的几何内核）。

**键设计**：四个「定义」三段键（线性相关/极大无关组/向量组的秩/内积——数一数二分节不同）；
「单位向量」「投影」与高数向量几何章撞名限定「向量/…」；含字母下标的标题
'β=k1α1+...+knαn'、含括号/斜杠/冒号的标题全部加引号；数一「求法」「常用性质」与数二
同名同章共用一份。

### 量化进展

| 指标 | 起点 | 矩阵章后 | **向量章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 688 | **728 / 960** |
| 唯一摘要 | 118 | 712 | **746** |
| 模板重复率 | 87.7% | 25.8% | **22.3%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 21 批：线代第 4 章「线性方程组」（数一 20 + 数二 18 = 38 点）

内容覆盖：克拉默法则（使用条件、xᵢ=Dᵢ/D 公式）、矩阵形式（Ax=b、增广矩阵）、
齐次方程组（Ax=0、零解、非零解存在条件 r(A)<n、基础解系、通解）、非齐次方程组
（有解条件 r(A)=r(A,b)、唯一/无穷多/无解三分类）、解的结构（齐次解组合构成解空间、
两特解之差是齐次解、通解=特解+齐次通解）、高斯消元与参数化通解。

**键设计**：「通解」限定「线性方程组/通解」（与常微分方程章撞名——两边内容完全不同，
一个带 n−r 个常数、一个带 n 阶导数）；数二的「使用条件」「公式」分别限定（与微分学
洛必达的使用条件、积分学的公式撞名）；「Ax=0」「Ax=b」「r(A)<n」等含等号的键加引号。

### 量化进展

| 指标 | 起点 | 向量章后 | **线性方程组后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 728 | **755 / 960** |
| 唯一摘要 | 118 | 746 | **765** |
| 模板重复率 | 87.7% | 22.3% | **20.3%** |

校验全绿：check-math-content 41 条、全套件、validate:content。

### 精编内容 第 22 批：线代第 5 章「特征值与特征向量」（数一 24 + 数二 20 = 44 点）

内容覆盖：定义（Aα=λα、α≠0、几何意义）、求法（特征方程 |λE−A|=0、特征多项式、
解 (A−λE)x=0、代数重数与几何重数、特征子空间）、性质（迹=Σλ、行列式=∏λ、幂 λᵏ、
逆 1/λ、f(A)=f(λ) 思想）、相似（定义式 B=P⁻¹AP、等价关系、相似不变量）、相似对角化
（定义、充要条件：n 个无关特征向量 ⇔ 几何重数=代数重数、重根检查）、实对称矩阵
（全实特征值、不同特征值正交、必可正交对角化、QᵀAQ=Λ 全流程）。

**键设计**：「几何意义」限定（与高数撞名）；「相似对角化/定义」三段键；数二短标题
（迹/行列式/幂/逆矩阵）全库唯一不加限定；两册措辞不同的标题（特征方程 |λE-A|=0 vs
|λE-A|=0、同一特征值对应特征子空间 vs 特征子空间、重根情形的判断 vs 重根情况等）
各写各键。新踩的语法坑：「n 个线性无关特征向量」键以字母开头但含空格，同样要加引号；
「与矩阵行秩/列秩的关系」在向量章补写时与前一份重复（TS1117 拦下）。

### 量化进展

| 指标 | 起点 | 线性方程组后 | **特征值章后** |
|---|---|---|---|
| 精编覆盖 | 0 / 960 | 755 | **793 / 960** |
| 唯一摘要 | 118 | 765 | **798** |
| 模板重复率 | 87.7% | 20.3% | **16.9%** |

校验全绿：check-math-content 41 条、全套件、validate:content。
